# XL Update Tool 开发文档指南

## 项目概述

XL Update Tool 是一个基于 PySide6 的桌面应用，用于下载和浏览游戏资源包。

### 功能特性

- 版本更新检测与下载
- 资源包全量/增量下载
- 资源包浏览与管理
- 下载进度实时显示
- 日志记录与错误追踪
- 自动导出资源到 material 目录
- 批量预览图片（使用 SpineViewerCLI 导出 .skel 为 PNG）
- 皮肤（Skin）导出：从 .skel 二进制提取 `motion_*` 名称，逐个导出各表情独立图片
- 角色图片预览（多选[Ctrl/Shift] + 拖放复制 + 自适应菜单[单选/多选/合成图] + 批量导出 + 导出参数对话框 + 配对合成 + 多动画导出）
- 合成图视频导出（SpineViewerCLI 直接导出 MP4 + FFmpeg colorkey 叠加合成）
- 预乘 Alpha（--pma）选项支持
- 音频管理器（.bank 解密 + 音频播放 + 批量导出）
- 音频分类导出：语音按角色（中文/日语）+ 音乐按专辑（星落/旅途/百折千回/危机四伏/旅途轶事）
- 导入AS 可配置导出：lua / 角色立绘 / FGUI图集 / 音频 四类勾选，按 Container 前缀严格区分
- 版本备注 delta 显示：每个版本显示相对上一版本的「新增 / 移除 / 未变」
- 工具栏：顶部视图栏（版本列表/图片预览/音频/角色）+ 侧边功能栏（检查更新/导入AS/刷新/作者 + 导出配置）

## 项目架构

```
xl_updata_tool/
├── main.py                 # 应用入口
├── requirements.txt        # 依赖列表
├── run.bat                 # Windows 启动脚本
├── app/                    # 应用核心代码
│   ├── __init__.py
│   ├── core/               # 核心模块
│   │   ├── __init__.py
│   │   ├── downloader.py   # HTTP、版本信息和校验函数（无 Qt）
│   │   ├── database.py     # 数据库管理
│   │   ├── version_manager.py  # 版本管理
│   │   ├── local_bundle_sync.py # 磁盘 Bundle 与数据库状态同步
│   │   ├── character_cache.py  # 角色缓存与表格索引派生
│   │   ├── character_presenter.py # 角色详情 HTML 与 CSV 导出
│   │   ├── audio_library.py # 音频目录扫描、导出与格式化
│   │   ├── preview_catalog.py # 预览图与 Spine 路径匹配
│   │   ├── seed_versions.py # 内置版本种子注册
│   │   ├── version_cleanup.py # 已下载版本文件清理
│   │   ├── bundle_parser.py    # 资源包解析（含 compute_delta）
│   │   ├── bundle_manager.py   # 资源包管理
│   │   ├── album_map.py    # 音乐专辑映射（bgm 文件名 → 专辑名）
│   │   ├── character_loader.py # 角色 Lua 数据解析引擎
│   │   └── logger.py       # 日志系统
│   └── ui/                 # UI 模块
│       ├── __init__.py
│       ├── main_window.py  # 主窗口骨架（~800行，仅保留视图切换、版本列表和事件分发）
│       ├── panels.py       # 面板组件
│       ├── theme.py        # 主题样式（含 THUMB_SIZE 常量）
│       ├── asset_browser.py # 资源浏览器
│       ├── legacy/          # 旧流程兼容入口（不属于主流程）
│       │   ├── __init__.py
│       │   └── asset_browser_entry.py
│       ├── bundle_model.py # 资源包数据模型
│       ├── features/       # 功能模块（无 Qt 依赖的纯 Python 解析逻辑）
│       │   ├── __init__.py
│       │   └── fgui_atlas.py  # FGUI 图集解析（ByteBuffer/UIPackage/UIPackageTool）
│       ├── dialogs/        # 对话框组件
│       │   ├── __init__.py
│       │   ├── image_viewer.py  # 大图预览窗口（ImageViewerDialog）
│       │   └── export_settings.py  # 导出参数设置（ExportSettingsDialog）
│       ├── widgets/        # 自定义控件
│       │   ├── __init__.py
│       │   └── drag_list.py  # 支持拖放复制（DragListWidget）
│       ├── workers/        # 后台工作线程（QThread）
│       │   ├── __init__.py
│       │   ├── download.py     # 下载/检查更新 Qt 线程包装
│       │   ├── image_loader.py  # 缩略图异步加载（ImageLoadWorker）
│       │   ├── preview_export.py  # 图片预览导出（PreviewExportWorker）
│       │   ├── audio_decrypt.py  # 音频解密（AudioDecryptWorker）
│       │   ├── lua_decrypt.py    # Lua 字节码解密（LuaDecryptWorker + FIXED_HEAD）
│       │   ├── import_as.py      # 导入 AS 资源（ImportASWorker）
│       │   ├── batch_export.py   # 批量动画导出（BatchExportWorker）
│       │   └── composite_export.py  # 批量合成图导出（CompositeExportWorker）
│       └── adapters/       # 适配器（静态工具方法）
│           ├── __init__.py
│           └── spine_adapter.py  # Spine/FFmpeg 工具函数（配对、合成、动画名、视频导出等）
├── data/                   # 数据目录（通过 get_data_dir() 获取，始终指向项目根目录下的 data/）
│   ├── bundles/            # 下载的资源包
│   ├── material/           # 导出的素材文件（.skel/.atlas/.bank/.bytes 等）
│   ├── seeds/              # 种子版本文件
│   └── xl_updata.db        # SQLite 数据库
├── output/                 # 输出目录（通过 get_output_dir() 获取）
│   ├── character/          # 图片预览导出（PNG、GIF），按角色编号分目录 <角色ID>/
│   ├── fgui/               # FGUI 图集切割输出，按包名分目录 <包名>/
│   ├── video/              # 视频导出（MP4）
│   └── audio/              # 音频解密输出（.wav/.ogg/.mp3）
│       ├── voice/<角色ID>/<cn|jp>/   # 角色语音（中文/日语）
│       └── album/<专辑名>/           # 音乐专辑（未分类 bgm 归 album/未分类/）
├── logs/                   # 日志目录（通过 get_logs_dir() 获取，自动创建）
└── tools/                  # 工具目录（通过 get_tools_dir() 获取路径）
    ├── AssetStudio/        # AssetStudio CLI 工具（路径：os.path.join(get_tools_dir(), "AssetStudio", "AssetStudio.CLI.exe")）
    ├── SpineViewer/        # SpineViewer + SpineViewerCLI + ffmpeg.exe
    └── epic7_debank_v1_0/  # 音频 .bank 解密工具
        ├── epic7_debank.py # 解密主脚本（支持 -i/-o 参数）
        ├── _subcontractors/# quickbms.exe + bank-files.bms + fsb_aud_extr.exe
        ├── input/          # 输入目录（.bank 文件）
        └── result/         # 中间结果目录
```

## 核心模块说明

### core/downloader.py 与 ui/workers/download.py

下载逻辑按依赖方向拆成两层：

- `app/core/downloader.py`：无 Qt 的 HTTP 请求、版本信息获取、Bundle 校验函数；
- `app/ui/workers/download.py`：`CheckUpdateThread` 和 `DownloadWorker` 的 Qt 生命周期、进度信号和错误信号。

`DownloadWorker` 保持原有信号兼容：`progress`、`item_done`、`item_skip`、`item_fail`、`all_done`、`error`；网络失败和 MD5 不匹配最多重试 3 次。下载数据先写入同目录临时文件，修复成功后再原子替换最终 Bundle，失败不会覆盖已有可用文件。

### core/database.py

SQLite 数据库管理，存储版本信息和资源包路径。

### core/logger.py

日志系统，配置：
- 每次启动独立日志文件：`logs/app_YYYYMMDD_HHMMSS_ffffff.log`
- 单文件最大 10 MB（`maxBytes=10*1024*1024`），超出时自动清空重写
- 文件日志不可用时保留控制台日志，并输出降级警告
- 文件日志级别 DEBUG，控制台日志级别 INFO
- 编码 `utf-8`，支持中文字符
- 同时输出到控制台和文件

### core/character_loader.py

角色 Lua 数据解析引擎（无 Qt 依赖，纯模块级函数）。从 `output/lua/` 解密后的 Lua 文件中提取结构化角色数据，供主窗口 `_load_character_data` 调用。主要函数：
- `parse_word_file(file_path)` - 解析 `BaseWord_cn.lua` 文本映射（id → 文本）
- `parse_cv_file(file_path)` - 解析配音 `BaseCvNameCn.lua`
- `parse_level_up_file(file_path)` - 解析 `BaseCardLevelUp.lua` 升级属性
- `parse_quality_up_file_with_cost(file_path)` / `parse_skill_level_up_file_with_cost(file_path)` - 解析品质/技能升级消耗
- `parse_badge_suit_file(file_path, word_dict)` / `parse_item_file(file_path, word_dict)` - 解析套装与物品
- `parse_basecard_file(file_path, word_dict, cv_dict, level_up_dict, quality_up_dict, skill_file_path, skill_level_up_file_path, badge_suit_dict)` - 完整解析 `BaseCard.lua`，返回角色数据字典
- 技能/突破相关：`extract_skill_info`、`extract_awakening_skill_info`、`extract_awakening_info`、`extract_multi_level_skill_info_new`、`get_breakthrough_cost`、`get_normal_skill_upgrade_cost`、`get_passive_skill_upgrade_cost`
- 块提取与 T() 引用解析：`extract_all_card_blocks`、`extract_t_references`、`parse_t_args`、`resolve_t_call`、`parse_t_function_params`、`process_t_function_params`、`merge_t_function_params`
- `load_character_data(lua_dir, progress_callback=None)` - 角色数据整体加载（纯解析入口），解析全部 Lua 文件并返回 `(characters, characters_full, word_map)`

### 调用关系（第二轮重构后）

`_load_character_data` 保留在 `MainWindow` 中作为 UI 协调层，职责：
- 管理进度条和状态栏更新
- 调用 `app.core.character_loader.load_character_data` 执行实际解析
- 将解析结果赋值给 `self.characters`、`self.characters_full`、`self.word_map`
- 调用 `_populate_character_table` 刷新表格

**解析逻辑**：`app/core/character_loader.py`（无 Qt 依赖，可独立测试）

**调用链路**（`output/` 为显示数据单一事实源）：
用户点击【角色】或【开始解析】
↓
`_load_character_data()` ← UI 协调层（读 `output/lua`，不再读 `data/material/assets/lua`）
↓
先读 `output/character_data/characters_full.json` 缓存；源文件 mtime 变化才重解析
↓
`character_loader.load_character_data(output/lua)` ← 纯解析引擎（模块函数）
↓
返回 `(characters, characters_full, word_map)`；完整数据（`characters_full`）存 `output/character_data/characters_full.json`
↓
从 `characters_full` 派生表格索引 `characters`，更新 UI（进度条、表格、右侧详情）

> 说明：AS 导入时 `ImportASWorker` 已把反编译后的 `.lua` 同步到 `output/lua/`；app 只从 `output/` 读取，与 AS 导出解耦。旧版 `LuaDecryptWorker`（unluac 回退解密）已从 `MainWindow` 移除。

### ui/main_window.py

主窗口控制器目前仍是较大的协调层，正在按领域逐步收口；目标是只保留：
- 窗口骨架创建与初始化
- 工具栏按钮事件分发
- 版本列表展示与下载管理
- 视图切换逻辑（版本列表 / 图片预览 / 音频管理器 / 角色数据）
- 子模块导入与协调

后台工作线程、对话框、自定义控件、FGUI 解析工具、Spine/FFmpeg 适配器已拆分为独立模块；版本/下载之外的主窗口领域协调逻辑仍按后续阶段继续迁移（见 `架构与协作基线.md`）。

### ui/ 子目录职责说明

| 子目录 | 职责 | 主要文件 |
|--------|------|----------|
| `features/` | 无 Qt 依赖的纯 Python 功能模块 | `fgui_atlas.py`（图集解析：ByteBuffer、UIPackage、UIPackageTool）、`export_controller.py`（导出流程控制：单文件/批量/合成图导出） |
| `views/` | 视图构建函数（返回容器 + 控件字典） | `preview_view.py`（图片预览）、`audio_view.py`（音频管理器）、`character_view.py`（角色数据） |
| `dialogs/` | 独立对话框窗口 | `image_viewer.py`（大图预览）、`export_settings.py`（导出参数设置） |
| `widgets/` | 自定义 UI 控件 | `drag_list.py`（支持拖放复制的列表控件） |
| `workers/` | 后台工作线程（QThread） | 7 个 Worker 线程，分别处理图片加载、预览导出、音频解密、Lua 解密、AS 导入、批量导出、合成图导出 |
| `adapters/` | 静态工具方法适配器 | `spine_adapter.py`（Spine/FFmpeg 工具函数集合） |

### ui/asset_browser.py

资源浏览器窗口，包含：
- `_FixWorker` - 文件修复线程（复制并修复文件头，支持大小去重）
- `_MapWorker` - AssetStudio 解析线程（生成资源映射）
- `_ExtractWorker` - 资源导出线程
- 实时进度显示（修复进度 + 加载进度）
- 资源筛选、预览、导出功能

## 资源浏览流程

> **v1.21.0 更新**：导入AS 已集成到主界面，不再弹出独立的资源浏览器窗口。
> 所有操作（修复、解析、导出分类）在主界面后台执行，进度通过下载进度条和状态栏显示。

点击"导入AS"后，执行以下流程（全部在 `ImportASWorker` 后台线程中执行，不阻塞 UI）：

1. **获取版本文件列表**：
   - 从数据库获取选中版本的所有 Bundle 本地路径
   - 如果文件列表为空，提示"此版本没有已下载的 bundle，请先下载"
2. **阶段 1：修复文件头**（`_stage_fix`）：
   - 直接对原始 `.bundle` 文件调用 `fix_bundle_inplace`（不再复制到 `_as_temp`）
   - 实时更新进度条："修复文件头: X/总数"
   - 单个文件修复失败时记录日志并跳过，不中断整体流程；只有 `fix_bundle_inplace()` 明确返回成功才计入成功数
   - 如果所有文件修复失败，提示"所有文件修复失败，请删除该版本并重新下载"
3. **阶段 2：解析资源**（`_stage_map`）：
   - 调用 AssetStudio CLI 生成资源映射（`--map_op Both --map_type JSON`）
   - 实时更新进度条："解析资源: X/总数"
   - 输出 `assets_map.json` 到 `data/bundles/{ts}/_map/`
4. **阶段 3：导出分类**（`_stage_export`）：
   - 先写入 `data/.xl-import-*/material/` 临时目录，全部分类成功后再替换 `data/material/` 对应分类
   - 按「侧边栏勾选的分类」（lua / 角色立绘 / FGUI图集 / 音频）逐个调用 AssetStudio CLI 导出，每类用 `--types` + `--containers` 严格匹配（见 `EXPORT_CATEGORIES` 常量）
   - 实时更新进度条："导出分类: X/分类总数"
   - 若导出了 lua 分类，在临时目录中反编译 `.lua.bytes → .lua`，提交成功后再同步到 `output/lua/`
   - 清理 `.prefab` 后缀（AssetStudio CLI 可能错误添加）
   - 统计导出文件总数；AssetStudio 局部资源报错但目标分类已有产物时允许提交并记录警告，分类完全无产物或任务取消时清理临时目录并保留旧产物
5. **完成提示**：
   - 成功：弹窗"导入完成！文件已分类到 data/material/（共 N 个文件）"
   - 失败：弹窗"文件可能损坏，请点击【删除已下载】并重新下载"

### 进度显示

| 阶段 | 进度条格式 | 状态栏 |
|------|-----------|--------|
| 修复文件头 | `修复文件头: 150/2956` | `导入AS: 修复文件头 150/2956` |
| 解析资源 | `解析资源: 50/200` | `导入AS: 解析资源 50/200` |
| 导出分类 | `导出分类: 3/8` | `导入AS: 导出分类 3/8` |

### 备用资源浏览器

`_browse_version` 方法保留为备用（当前不调用），通过 `ui/legacy/asset_browser_entry.py` 打开独立的 `AssetBrowser` 窗口。旧窗口不参与主流程，后续可单独维护或移除。

## 图片预览与导出流程

### 图片预览流程

点击【图片预览】按钮后：

1. **切换视图**：隐藏版本列表表格，显示图片预览视图
2. **缓存检查**：检查 `output/character/` 目录是否已有 PNG 图片
   - 已有图片：直接加载（日志：`预览目录已存在 X 张图片，直接加载`，状态栏显示 `来源：缓存`）
   - 无图片：启动后台导出线程后加载（状态栏显示 `来源：刚导出`）
3. **图片导出**（`PreviewExportWorker` 后台线程）：
   - 在后台线程执行，不阻塞 UI（进度条实时反馈 `导出中... X/总数`）
   - 扫描 `data/material/` 目录中的 `.skel` 文件
   - **去重优化**：`force=False` 时，若 `output/character/{base_name}.png` 已存在且大小 > 0，则跳过该文件
   - **强制刷新**：点击【🔄 重新加载图片】时清空目录并 `force=True` 重新导出
   - 对每个 `.skel` 执行以下两步导出：
     - **动画帧导出**（`export_animation_frames`，位于 `adapters/spine_adapter.py`）：
       - 从 `.skel` 二进制文件提取动画名称（正则 `motion_[a-zA-Z0-9_]+`）
       - 遍历每个动画，使用 SpineViewerCLI 导出第一帧 PNG
       - 命令：`export -f Png --time 0 --duration 1 --fps 1 --scale 4 --max-resolution 8192 --pma`
       - 支持 `--pma` fallback：带 `--pma` 失败时自动重试不带 `--pma`
     - **皮肤图片导出**（`export_skel_skins`，位于 `adapters/spine_adapter.py`）：
       - 从 `.skel` 二进制文件提取 `motion_*` 名称作为皮肤列表
       - 遍历每个皮肤，使用 SpineViewerCLI 导出独立 PNG
       - 动画固定为 `idle`，添加 `--skins {skin_name}` 参数
       - 输出文件名：`{base_name}_{skin_name}.png`
       - 支持 `--pma` fallback
   - **配对合成**：识别 `xxx.skel + xxx_bg.skel` 配对，合成 `{xxx}_composite.png`（去重：已存在则跳过）
   - 信号：`progress(int, int)` / `export_finished(bool, str)` / `error(str)`
   - 支持取消：切换视图时调用 `cancel()` 终止后台任务
4. **缩略图显示**：网格展示，支持 Ctrl/Shift 多选、拖放复制

### 图片预览右键菜单

| 操作 | 单选 | 多选 | 合成图 |
|------|------|------|--------|
| 📂 打开文件所在目录 | ✅ | ❌ | ✅ |
| 📋 复制文件 | ✅ | ❌ | ✅ |
| 🎞️ 导出 GIF / 批量导出 GIF | ✅ | ✅ (N个) | ✅ |
| 🎬 导出为视频 / 批量导出视频 | ✅ | ✅ (N个) | ✅ |

### 视频导出流程

#### 普通视频导出（`_export_with_dialog`）

1. 弹出 `ExportSettingsDialog` 对话框
2. 用户选择：格式（MP4/GIF）、动画、时长、帧率、缩放、预乘 Alpha
3. 从 PNG 文件名提取皮肤名（`motion_*`），如有则添加 `--skins` 参数
4. 构建 SpineViewerCLI 命令：
   ```
   SpineViewerCLI.exe export <skel> -f Mp4/Gif -o <output> -a <animation>
     --atlas <atlas> --duration <dur> --fps <fps> --scale <scale>
     [--pma] [--skins <skin_name>]
   ```
5. 输出到 `output/video/`（MP4）或 `output/character/`（GIF）

#### 合成图视频导出（`_export_composite_video`）

采用 **分别导出角色和背景 MP4 → FFmpeg 叠加合成** 的方案：

1. 从合成图文件名（如 `xxx_composite.png`）解析基础名 `xxx`
2. 在 `self._skel_map` 中查找 `xxx.skel`（角色）和 `xxx_bg.skel`（背景）
3. 弹出 `ExportSettingsDialog` 对话框获取参数
4. 调用 `_export_composite_video_with_params` 执行：
   - **步骤 1**：导出角色 MP4（`role_temp.mp4`）
     - 使用 SpineViewerCLI `export -f Mp4`，添加 `--color "#00000000"` 透明背景
     - 根据 `skin_name` 参数决定是否添加 `--skins`
   - **步骤 2**：导出背景 MP4（`bg_temp.mp4`）
     - 不应用皮肤参数
   - **步骤 3**：FFmpeg 叠加合成
     - 调用 `ffmpeg_composite_videos` 函数（位于 `adapters/spine_adapter.py`）
     - FFmpeg 路径通过 `get_ffmpeg_path()` 获取（`adapters/spine_adapter.py`，优先 `tools/SpineViewer/ffmpeg.exe`）
     - MP4 滤镜：`colorkey=0x000000:0.1:0.2`（去除角色视频黑色背景）
     - 完整命令：
       ```
       ffmpeg -y -i bg_temp.mp4 -i role_temp.mp4
         -filter_complex "[1:v]colorkey=0x000000:0.1:0.2[role];[0:v][role]overlay=0:0"
         -c:v libx264 -pix_fmt yuv420p -crf 23 -r {fps} output.mp4
       ```
     - GIF 合成使用 `palettegen/paletteuse` 滤镜保持透明度
5. 输出到 `output/video/`，文件名：`{base_name}_composite_{timestamp}.mp4`
6. 清理临时目录（`output/temp/`）

#### 批量导出（`_batch_export_with_dialog`）

1. 从 `_show_context_menu` 接收 entries（3 元组：`skel, atlas, png_path`）
2. 按是否为合成图分类：`composite_pngs` 和 `regular_entries`
3. **只弹一次** `ExportSettingsDialog` 获取参数
4. 确认对话框显示分类统计（`普通: N，合成图: M`）
5. **合成图**：通过 `CompositeExportWorker` 后台线程串行处理（不阻塞 UI）
   - 信号：`progress(int, int, str)` / `one_finished(str, bool)` / `all_finished(int, int)`
   - 合成图导出完成后自动启动普通文件导出
6. **普通文件**：通过 `BatchExportWorker` 后台线程批量处理
   - 两者结果在 `_on_regular_all_finished` 中合并统计

#### ExportSettingsDialog 参数

| 参数 | 选项 | 默认值 |
|------|------|--------|
| 输出格式 | MP4 / GIF | 取决于调用来源 |
| 动画名称 | 手动输入 | idle |
| 时长 | 1-10 秒 | 2 |
| 帧率 | 5-60 fps | 15 |
| 缩放 | 1-8x | 2 |
| 预乘 Alpha | 勾选框 | ✅ 勾选 |
| 导出后自动打开 | 勾选框 | ❌ 不勾选 |

## 音频管理器流程

### 总体流程

点击【音频】按钮后，自动执行以下流程（全部在 `AudioDecryptWorker` 后台线程中执行，不阻塞 UI）：

```
用户点击【音频】
       │
       ▼
切换到音频管理器视图（隐藏版本列表）
       │
       ▼
启动 AudioDecryptWorker 后台线程
       │
       ▼
【步骤 1：转换 .bytes → .bank】
  ├── 扫描 data/material/ 目录（递归）
  ├── 筛选规则（两者需同时满足）：
  │   ├── 文件所在路径包含 fmodassets/ 子目录
  │   └── 语音（btl/system，纯数字名如 012.bytes）或 bgm（位于 bgm/ 子目录下）
  ├── 不满足条件的 .bytes 文件完全忽略（sfx/trigger/sty/master 未被导出，也不会被处理）
  ├── 将符合条件的 .bytes 文件重命名为 .bank（原地重命名）
  ├── 复制到解密工具 input 目录（去重：大小一致则跳过复制）
  └── 记录日志：扫描到音频 .bytes 文件: X 个（跳过 Y 个非音频文件）
       │
       ▼
【步骤 2：解密 .bank 文件】
  ├── 增量检查：即使 output/audio/ 已有音频，也继续扫描当前 bank；底层按目标文件大小去重
  ├── 构建 bgm → 专辑名映射（app/core/album_map.py，复用反编译的 BaseSound/BaseSoundChapter/BaseWord_cn）
  ├── 调用 epic7_debank.py run(material_dir, output_dir, subdir_fn=_audio_subdir)
  ├── 脚本内部流程：
  │     ├── 递归扫描输入目录，收集所有 .bank 文件
  │     ├── 遍历每个 .bank 文件，通过 subdir_fn 决定输出子目录
  │     ├── 对每个 .bank 调用 quickbms.exe 解包
  │     ├── quickbms 通过 -S 回调 _epic7_defsb.py 提取音频
  │     ├── 将提取的音频文件复制到 subdir_fn 返回的子目录下
  │     │   └── 去重：目标文件存在且大小一致时跳过
  │     └── 清理本次 result 目录，避免影响下一个 bank
  └── 记录日志：解密完成，提取 Y 个音频文件

**输出分类说明**（`_audio_subdir`）：解密后的音频按两类分目录，不再平铺或按原目录结构：
- 语音 → `output/audio/voice/<角色ID>/<cn|jp>/`（`voice_jp`→jp，`voice_cn`→cn；角色 ID 即语音 bank 的数字名，如 001、078、121）
- bgm → `output/audio/album/<专辑名>/`（星落/旅途/百折千回/危机四伏/旅途轶事；未匹配专辑的 bgm 归 `album/未分类/`）
       │
       ▼
finished_decrypt 信号 → 自动调用 _load_audio_list()
       │
       ▼
【步骤 3：加载音频列表】
  ├── 扫描 output/audio/ 目录中的 .wav/.ogg/.mp3
  ├── 填充表格（文件名、时长、格式、大小）
  └── 状态栏显示：共 X 个音频文件
```

**信号**：`progress(str)` / `finished_decrypt()` / `error(str)`
**取消机制**：切换视图时调用 `cancel()` 终止后台任务

### 音频管理器 UI 组件

| 组件 | 说明 |
|------|------|
| 顶部栏 | 标题"🎵 音频管理器" + 关闭按钮 |
| 工具栏 | 🔄 刷新列表 / 📤 导出选中 / ▶ 播放选中 |
| 表格 | 复选框 / 文件名 / 时长 / 格式 / 大小 |
| 播放控制区 | 播放/暂停按钮 + 文件名 + 进度条 + 时间 + 音量 |
| 状态栏 | `已选: X 个 \| 共 X 个音频文件` |

### 音频交互功能

| 操作 | 说明 |
|------|------|
| 刷新列表 | 重新扫描 `output/audio/`，更新音频列表 |
| 导出选中 | 弹出目录选择对话框，复制选中音频文件 |
| 播放选中 | 使用 QMediaPlayer 播放选中的第一个音频文件 |
| 双击播放 | 双击表格行播放该音频 |
| 右键菜单 | 打开文件所在目录 / 复制文件 / 播放 |

### epic7_debank.py 脚本说明

- **位置**：`tools/epic7_debank_v1_0/epic7_debank.py`
- **用法**：`python epic7_debank.py -i <input_dir> -o <output_dir>`
- **依赖工具**（位于 `_subcontractors/` 目录）：
  - `quickbms.exe` - Bank 文件解包工具
  - `bank-files.bms` - quickbms 脚本
  - `fsb_aud_extr.exe` - FSB 音频提取工具
  - `_epic7_defsb.py` - quickbms 回调脚本
- **特性**：
  - 支持 `-i`/`-o` 命令行参数指定输入/输出目录
  - 递归扫描子目录中的 `.bank` 文件
  - 每个 bank 文件独立临时子目录，避免冲突
  - **自定义输出子目录**：`run()` 新增 `subdir_fn(rel_path, bank_stem)` 参数，调用方自定义输出子目录（默认回退保留原目录结构），同名文件互不覆盖
  - **子目录级别去重**：目标文件存在且大小一致时跳过，避免重复处理
  - 处理完成后自动清理临时目录

## 导出资源目录结构

资源导出功能将 Unity 资源提取到 `data/extracted/` 目录下，目录结构如下：

```
data/extracted/
├── assets/
│   ├── art/
│   │   ├── effects/
│   │   │   └── prefab/
│   │   │       ├── ui_prefab/mapadventure/
│   │   │       └── ui_prefab/storymusicpic/
│   │   └── models/
│   │       ├── anicg/prefab/
│   │       ├── battlespine/
│   │       │   ├── battlespine_20716/
│   │       │   ├── battlespine_20717/
│   │       │   ├── battlespine_20910_1_body/
│   │       │   ├── battlespine_20910_body/
│   │       │   └── prefab/
│   │       ├── cardspine/prefab/
│   │       ├── dialoghead/prefab/
│   │       ├── npcspine/
│   │       │   └── npcspine_XXXXXXX/referenceassets/
│   │       └── ui_spine/
│   │           ├── chapter/open/
│   │           ├── expedition/
│   │           ├── prefab/
│   │           │   ├── eventcovers/
│   │           │   ├── minigame/
│   │           │   └── solobosschallenge/
│   │           ├── reason/
│   │           └── tradeunionwar_cover_spine/
│   ├── fairygui/
│   │   └── ui/
│   └── fmodassets/
│       ├── bgm/
│       │   ├── bgm_btl/
│       │   ├── bgm_story/
│       │   └── bgm_system/
│       ├── sfx/
│       │   ├── sfx_amb/story/
│       │   └── sfx_btl/
│       │       ├── boss/
│       │       ├── buff/
│       │       └── npc/
│       ├── trigger/
│       ├── voice_cn/
│       │   ├── btl/
│       │   ├── sty/
│       │   └── system/
│       └── voice_jp/
│           ├── btl/
│           ├── sty/
│           └── system/
```

### 文件类型说明

| 文件类型 | 数量 | 说明 |
|----------|------|------|
| `.prefab` | 525 | Unity 预制体文件，包含游戏对象配置 |
| `.bytes` | 370 | 二进制数据文件（主要是音频资源） |
| `.asset` | 30 | Unity 资产文件，存储资源元数据 |
| `.txt` | 9 | 文本配置文件 |
| `.json` | 2 | JSON 配置文件 |

### 目录分类说明

- **art/effects/**：特效资源
- **art/models/**：模型资源（角色、卡片、NPC、UI spine 动画等）
- **fairygui/ui/**：FairyGUI UI 资源
- **fmodassets/bgm/**：背景音乐（战斗、剧情、系统）
- **fmodassets/sfx/**：音效（环境音、战斗音效、Buff 音效、NPC 音效）
- **fmodassets/voice_cn/**：中文语音（战斗、剧情、系统）
- **fmodassets/voice_jp/**：日文语音（战斗、剧情、系统）

## 日志系统

### 使用方式

```python
from app.core.logger import logger

logger.info("操作信息")
logger.warning("警告信息")
logger.error("错误信息")
logger.debug("调试信息")
```

### 日志文件

日志文件位于 `logs/app.log`，使用 `RotatingFileHandler` 限制大小：
- 单文件最大 10 MB（`maxBytes=10*1024*1024`）
- 备份数量 0（`backupCount=0`），仅保留当前文件，超出大小时自动清空重写
- 文件日志级别 DEBUG，控制台日志级别 INFO
- 编码 `utf-8`

## 运行方式

```bash
# 安装依赖
pip install -r requirements.txt

# 运行应用
python main.py

# 或使用批处理脚本
run.bat            # 正常模式
debug.bat          # 调试模式（含调试工具栏，见下文）
```

## 调试模式

调试模式用于开发测试，通过 `debug.bat` 或 `python main.py --debug` 启动。启动后工具栏下方会多出一排「调试模式」工具栏，提供两类功能：

### 选择性导出

> **v1.29.0 起**：导入AS 的导出范围已由侧边栏「导出配置」4 类勾选（lua / 角色立绘 / FGUI图集 / 音频）接管，正式模式与调试模式通用。下方调试模式旧「导出范围」下拉框已不再生效（`_get_export_filter` 不再被调用，仅保留 `export_types` 参数兼容），后续可考虑移除。

旧「导出范围」下拉框（调试模式）曾用于选择「导入AS」要导出的资源类型：

| 选项 | 导出类型 |
|------|---------|
| 全量导出 | 所有资源类型（默认） |
| 只导出 Lua | 仅 `TextAsset`（测试 Lua 反编译用） |
| 只导出贴图 | 仅 `Texture2D`（测试贴图导出用） |

### 清空数据区域

一排「清空」按钮，点击后弹确认框，确认后清空对应目录（清空后自动重建空目录）：

| 按钮 | 对应目录 |
|------|---------|
| 清空ab包 | `data/bundles/` |
| 清空AS导出 | `data/material/` |
| 清空立绘 | `output/character/` |
| 清空图鉴 | `data/material/assets/lua/`（Lua 图鉴数据） |
| 清空音频 | `output/audio/` |

## 版本历史

### v1.36.0 (2026-08-16)

**阶段 D（立绘拼接）+ 两轮测试反馈修复**

#### 1. 立绘拼接（UnityPy 提取背景偏移）

- 新增 `app/core/prefab_parser.py`：`parse_prefab` 用 UnityPy 读取 bundle 的 `Transform.m_LocalPosition` 识别背景/人物/mesh_part；`compute_pixel_offset` 算背景像素偏移；`build_cardspine_bundle_map` 从 assets_map.json 的 `Source` 字段建「角色→bundle」映射（290 个角色）。
- `spine_adapter.composite_with_offset`：背景按偏移贴、人物 (0,0) 对齐、不 resize 拉伸（修掉 G5 变形 + 背景不对齐）。
- `preview_export` 集成偏移合成 + 新增角色选择对话框 `dialogs/character_select.py`（勾选要导出的角色）。

#### 2. 测试反馈修复

- **浅色主题**：新增 `LIGHT_PALETTE` + `LIGHT_STYLESHEET`（浅灰底 + 深色文字 + 蓝强调），主题下拉框加「浅色」。
- **版本列表 checkbox 选中**：加 checkbox 列，点一下切换、Ctrl 多选。
- **角色数据本地缓存**：解析后写 `output/character_data/characters.json`，二次进入直接读缓存。
- **加载优化**：图片缩略图每 20 张 msleep 分批；音频列表改 QTreeWidget 树形（voice→角色编号→中日语；album→专辑）。
- **图片预览角色过滤**：加「角色」下拉框按角色过滤显示。
- **日志**：`logger.timed` 装饰器计操作耗时；按启动分文件 `app_YYYYMMDD_HHMMSS.log` + 保留最近 20 个 + 区分正常/debug 启动。
- **崩溃修复**：`_audio_player` 未初始化、`_side_btn` 传字符串给 setIcon、`QToolBar.addStretch` 不存在。

### v1.35.0 (2026-08-16)

**阶段 C（交互+产物）+ 阶段 B（UI 设计）**

#### 1. 阶段 C：交互 + 产物

- 手动开始按钮（角色「开始解析」、音频「开始解密」，切视图不再自动触发）。
- 进度弹窗加大（520×160）+ 两行信息（阶段名 / 已处理 x/y），避免「处理中/xxx」来回跳。
- 全量导出（>1000 bundle）弹「预计耗时较长」确认；导出分类完成实时刷新音频列表（边导出边预览）。
- 音频树形层级显示（加「目录」列，后改树形折叠）。
- lua 留存到 `output/lua/`、角色数据留存到 `output/character_data/`、更新日志 `output/CHANGELOG.md`。

#### 2. 阶段 B：UI 设计

- 侧边栏重排（导入AS 紧跟导出配置，刷新/作者用 stretch 推底部）、图标去重（刷新换 `arrow-rotate-right`）、按钮统一最小宽度、工具栏只在版本列表首页显示。
- 下载按钮改描边克制配色、删除按钮加宽修溢出。
- 新主题配色从「紫」换「蓝灰 #259ae9」。

### v1.34.0 (2026-08-16)

**大批改进·阶段 A：bgm debank 修复 + 音频弹窗取消 + 反编译进度实时（Issue #18）**

#### 1. bgm 没 debank 修复

- `tools/epic7_debank_v1_0/epic7_debank.py` `_should_process_file` 从「文件名纯数字 + fmodassets」改为「fmodassets 下 纯数字名 或 位于 bgm 目录」，与 `audio_decrypt.py` 筛选一致。修复 bgm 的 79 个 bank 被过滤（之前 log：503 个 .bytes → 424 个 .bank，只解了语音）。

#### 2. 音频解密弹窗「取消」按钮

- `main_window.py` `_start_audio_decrypt` 的 `QProgressDialog` 取消文字从空串 `""` 改为 `"取消"`，并接 `canceled` 信号到 `self._audio_worker.cancel`。

#### 3. 反编译进度条实时

- `import_as.py` `_decompile_lua` 的 `progress_cb` 解析「正在反编译 Lua: done/total」→ `progress_stage.emit("反编译 Lua", done, total)`，批量反编译真实进度实时走（之前只 log、进度条等完成才动）。

### v1.33.0 (2026-08-16)

**界面美化：新旧双主题（保留旧 + Dracula 新）+ 切换按钮 + qtawesome 图标（Issue #17）**

#### 1. theme.py 双主题

- 旧主题常量全部保留不动（`BASE_STYLESHEET` = 旧主题，现有 view 的 inline 引用不受影响）。
- 新增 `NEW_PALETTE`（Dracula 配色：主紫 `#bd93f9` + 副粉 `#ff79c6`，背景 4 档 `#1e1e2e / #25253a / #2e2e4a / #38385a`）。
- `_build_stylesheet(palette)` 复用 BASE_STYLESHEET 模板替换颜色 + 追加新控件样式；导出 `OLD_STYLESHEET` / `NEW_STYLESHEET`。
- `apply_theme(app, name)` 运行时切换（`app.setStyleSheet`）。

#### 2. 新主题控件样式补全

- QCheckBox/QRadioButton 自定义 indicator（圆角方块，checked 用主紫实心）。
- QComboBox 下拉箭头（自定义三角）。
- 输入框三态 border（默认 / hover 亮一档 / focus 粉 `#ff79c6`）。

#### 3. 侧边栏主题切换

- 侧边栏「导出配置」下加「主题」下拉框（旧主题 / Dracula 新主题），`_on_theme_changed` 调 `apply_theme` + QSettings 保存偏好；启动时读取偏好（默认旧主题）。

#### 4. qtawesome 图标

- `requirements.txt` 加 `qtawesome`。
- 主要按钮（检查更新/导入AS/刷新/作者/版本列表/图片预览/音频/角色）加 Font Awesome 图标（fa6s solid），`_tbtn` 新增 `icon` 参数 + `_icon(name)` helper。

> 后续渐进：各 view 里的 emoji（🎵🔒🔊）替换为 qtawesome 图标 + inline 样式收敛，让主题切换更彻底。

### v1.32.0 (2026-08-16)

**debug 全量/增量导入 + 侧边栏勾选 2×2 + 角色立绘贴图排除 _e/_n（Issue #16）**

#### 1. debug 模式全量/增量导入选择

- debug 工具栏新增「导入范围」下拉框：全量导入 / 增量导入（增量 = 本版本 hash - 上一版本 hash）。
- 提取 `_compute_delta_hashes(ts)` 复用增量算法，`_download_version` 的增量分支也改为复用。
- `_import_selected` 按选择过滤 bundle：增量导入时只取「增量 hash 对应的已下载文件」。
- 顺带删除死代码 `debug_export_scope`（「导出范围」下拉框）+ `_get_export_filter`（已被侧边栏 4 分类勾选取代）。

#### 2. 侧边栏「导出配置」勾选排 2×2

- `_action_toolbar` 用 `QGridLayout` 把 4 个 QCheckBox（lua/角色立绘/FGUI图集/音频）排成 2 行 2 列，按钮保持竖排。

#### 3. 角色立绘贴图排除 _e/_n 法线/发光贴图

- `EXPORT_CATEGORIES["character"]` 的 Texture2D 加负向断言：cardspine 用 `^(?!.*_[en]$)`、battlespine 用 `battlespine_1\d{4}(?!.*_[en]$)`，排除 `_e`/`_n` 结尾的法线/发光贴图（Unity 专用，Spine 拼图用不上）。
- 效果（全量 assets_map.json 实测）：cardspine 贴图 1286→561，battlespine 1xxxx 贴图 560→190，保留的贴图无 `_e`/`_n` 泄漏。

### v1.31.0 (2026-08-15)

**导入AS 进度反馈 + 分类目录精确清空 + 下载状态同步磁盘 + 角色立绘精确筛选 + 音频播放器 bug（Issue #14）**

#### 1. 音频播放器 bug 修复

- `main_window.py` `__init__` 补 `self._audio_player = None` / `self._audio_output = None`。之前点「音频」崩 `AttributeError`，导致 `_start_audio_decrypt` 没执行、音频 debank 失败、列表为 0。

#### 2. `_stage_export` 只清空本次分类目录

- 新增 `CATEGORY_DIRS` 映射（lua/character/fgui/audio → material 子目录）。`_stage_export` 从「rmtree 整个 material」改为「只 rmtree 本次勾选分类对应目录」，二次导出不再清空其他分类产物（如 lua 反编译结果）。

#### 3. 下载状态扫描同步

- 新增 `main_window.py` `_sync_local_bundles()`：扫描 `data/bundles/<ts>/` 实际 `.bundle` 文件（文件名即 hash），与 DB `sub_bundles` 对比，同步 `local_path/downloadable`（磁盘为准）。`_load_data` 开头 + `_import_selected` 取文件前调用。修复「磁盘有全量 12797 个但 app 只显示 368 个」的状态脱节。

#### 4. 导出/反编译真实进度 + 不定态兜底

- `_stage_export` 从 `subprocess.run(capture_output=True)` 改 `Popen` 流式读 stdout，解析 `Loading <bundle>` 行逐文件显示「导出 audio: x/N」（加载阶段真实进度），`Process Assets` 后切不定态。
- `_decompile_lua` 统计 `.lua.bytes`/`.lua.bank` 数量作 total，`file_done_cb` 逐文件 `emit("反编译 Lua", done, total)`，不再只显示 0/1。

#### 5. 角色立绘精确筛选

- `EXPORT_CATEGORIES["character"]` 从 `assets/art/models`（太宽泛）改为：
  - `cardspine`（全部 103 个角色卡面，无怪物）
  - `battlespine_1\d{4}`（角色战斗小人，编号 1xxxx）
  - 排除怪物（2xxxx）/boss（3xxxx）/npcspine/ui_spine/minigame022/dialoghead/anicg/background。`_e/_n` 是法线/发光贴图变体（保留，前缀仍匹配）。

#### 6. 日志记「清除了什么 / 导出了什么 / 输出了什么」

- `_stage_export` 清空分类目录时 log「清空 X 目录: <path>」，导出每个命令 log 完整命令 + 耗时，完成后 log「共导出 N 个文件到 <material_dir>」。

### v1.30.0 (2026-08-15)

**导入AS 卡 0/0 修复 + 全项目日志补全**

#### 1. 导入AS 按分类导出跳过全量映射（省约 2 分钟）

- `ImportASWorker.run`（`app/ui/workers/import_as.py`）：勾选分类导出时跳过「阶段 2 解析资源」（`assets_map.json` 生成），直接进入导出。映射对分类导出无用（命令直接从 `EXPORT_CATEGORIES` 构建），仅调试模式/备用资源浏览器仍生成映射。

#### 2. 修复「解析资源 0/0」进度卡死

- 根因：`_stage_map` 调 CLI 带 `--silent`（压掉进度输出），且代码在解析根本不存在的 `"Processed"` 字样（CLI 实际输出 `Loading <bundle>`）。
- 修复：去掉 `--silent`，改为按 `Loading <bundle>` 行计数展示加载进度；`Process Assets` 阶段切「处理中」不定态。`asset_browser.py _MapWorker` 同款修复。
- `_on_import_progress`：total=0 时显示「处理中...」而非「0/0」。

#### 3. 阶段 3 导出命令起止日志

- `_stage_export` 每个导出命令记录开始（完整命令）+ 完成（退出码 + 耗时），成功不再静默。

#### 4. 全项目日志补全

- **核心层**：`downloader.py` / `database.py` / `version_manager.py` / `bundle_manager.py` 补 logger（原来零日志），入口 log 参数、完成 log 计数；`bundle_parser.py` / `album_map.py` / `character_loader.py` 补入口/计数日志。
- **UI 层**：`main_window.py` 各入口（检查更新/下载增量全量/删除版本/种子/导出音频）补分支与结果日志；各 Worker `run()` 补入口参数 + 完成汇总；`fgui_atlas.py` 从 print 改为 logger。
- **工具层**：`epic7_debank.py` 加 `_log`（导入时走 app 日志，独立运行回退 print），`execute_quickbms_single` 检查 returncode 并记 warning。
- **导入入口**：`_import_selected` 日志明确写出「勾选导出分类」，日志可还原一次完整操作。

### v1.29.0 (2026-08-15)

**导入AS 可配置导出分类 + 音频按角色/专辑分类 + 图片/音频输出分目录 + 版本备注 delta（Issue #13）**

#### 1. 导入AS 可配置导出分类（Issue #13）

- **侧边功能工具栏新增 4 类导出勾选**（`main_window.py` `_action_toolbar` / `_get_export_categories`）：lua / 角色立绘 / FGUI图集 / 音频，默认全勾。
- **新增 `EXPORT_CATEGORIES` 常量**（`app/ui/workers/import_as.py`）：经全量 `assets_map.json`（44895 资源）实测分析，四类按 Container 前缀严格区分、互不重叠：
  - `lua` = `assets/lua`（TextAsset）
  - `character` = `assets/art/models`（TextAsset `.skel`/`.atlas` + Texture2D 贴图）
  - `fgui` = `assets/fairygui`（TextAsset `.fui` + Texture2D 图集贴图）
  - `audio` = `assets/fmodassets/voice_cn|voice_jp 的 btl|system` + `assets/fmodassets/bgm`（只导语音和 bgm，不含 sfx/trigger/sty/master）
- **`ImportASWorker` 新增 `export_categories` 参数**（保留 `export_types` 兼容调试模式），`_stage_export` 走 `_build_category_commands()` 按分类构建 CLI 命令。
- **未勾选任何分类时弹提示**，避免误导出空目录。

#### 2. 音频分类导出（Issue #13）

- **新增 `app/core/album_map.py`**：解析反编译的 `BaseSound.lua` + `BaseSoundChapter.lua` + `BaseWord_cn.lua`，构建 `bgm文件名 → 专辑名` 映射（`build_album_map`）。
- **`AudioDecryptWorker` 筛选规则更新**：原来只认「纯数字名」，现改为「fmodassets 下语音（纯数字名）+ bgm（位于 bgm 目录）」。
- **`_audio_subdir` 决定输出子目录**：
  - 语音 → `voice/<角色ID>/<cn|jp>/`（角色 ID 即数字名，`voice_jp`→jp，否则 cn）
  - bgm → `album/<专辑名>/`（未匹配专辑的 bgm 归 `album/未分类/`）
- **`epic7_debank.run` 新增 `subdir_fn` 参数**（`tools/epic7_debank_v1_0/epic7_debank.py`）：允许调用方自定义输出子目录，替代原来的「保留原目录结构」。
- 实测（全量包体）：语音 424 个 bank 分到 `voice/<ID>/<cn|jp>`；bgm 79 个中 69 个命中 5 张专辑（星落 3 / 旅途 8 / 百折千回 9 / 危机四伏 14 / 旅途轶事 35）、10 个（jingle/UI/新增曲）归 `未分类`。

#### 3. 角色立绘按编号分目录（F1）

- **新增 `extract_character_id`**（`app/ui/adapters/spine_adapter.py`）：从 skel base 名提取 5 位角色编号（`cardspine_10080_4` → `10080`），找不到则回退原 base 名。
- **`PreviewExportWorker` 导出到 `output/character/<角色ID>/` 子目录**：主图、皮肤图、合成图均按角色编号归入各自目录。
- **`ImageLoadWorker` 递归扫描子目录**：预览网格能显示分目录后的角色图。

#### 4. FGUI 图集切割分目录（F2）

- **`PreviewExportWorker._export_fgui_atlas` 重写**：从「只处理 `CardHeadBanner_fui` 单个图集」改为「遍历 `data/material/assets/fairygui/ui/` 下所有 `*_fui.bank` → 逐个切割」。
- **输出到 `output/fgui/<包名>/`**，不再把几百张小图标倾倒进预览根目录。

#### 5. 视图切换不再杀后台任务（B2）

- `_toggle_preview_mode` / `_toggle_audio_mode` / `_toggle_character_mode` 移除 `_cancel_*_worker()` 调用，任务与视图解耦，切换界面不再中断导出/解密。

#### 6. 导入AS / 音频解密进度弹窗（A1/A2）

- **导入AS 进度弹窗**（`main_window.py` `_import_selected`）：`QProgressDialog`（非模态，可取消），阶段名 + 当前/总数实时更新。
- **音频解密进度弹窗**（`main_window.py` + `audio_decrypt.py`）：`AudioDecryptWorker` 新增 `progress_value(current, total, msg)` 信号，`QProgressDialog` 显示解密数值进度。

#### 7. 工具栏拆分 + 高亮（E1）

- `_toolbar` 拆为 `_view_toolbar`（顶部：版本列表 / 图片预览 / 音频 / 角色）+ `_action_toolbar`（侧边竖排：检查更新 / 导入AS / 刷新 / 作者 + 导出配置）。
- 新增 `_set_active_view_btn` 统一管理视图按钮高亮；「检查更新」不再高亮。
- `theme.py` 新增 `QToolButton:checked` 高亮样式。

#### 8. 版本备注 delta 显示

- **新增 `_compute_version_deltas`**（`main_window.py`）：按时间升序对每个版本计算相对上一版本的 bundle 差异，复用 `bundle_parser.compute_delta`。
- **`_populate_table` 备注列**优先显示 `新增 X | 移除 Y | 未变 Z`，替代原来 4 个种子版本硬编码的「APK内置版本」等文案。

#### 9. 清空 ab 包同步数据库状态

- `_clear_dir`（`main_window.py`）清空 `data/bundles/` 后，同步 `UPDATE sub_bundles SET local_path=NULL, downloadable=0` 并刷新版本列表，版本下载状态不再与实际文件脱节。

### v1.28.0 (2026-08-15)

**P2 UX 改进（第一批）**

- **单文件导出/合成视频后台化**（B1/G2，`app/ui/features/export_controller.py`）：
  - `export_with_dialog` / `export_composite_video` 从同步 `subprocess.run` 改为复用 `BatchExportWorker` / `CompositeExportWorker` 后台执行，导出期间窗口不再冻结
  - 删除死代码 `export_composite_video_with_params` + 重复的命令构造
- **缩略图不裁剪**（G4，`app/ui/workers/image_loader.py`）：`KeepAspectRatioByExpanding`（填充裁剪）改为 `KeepAspectRatio`（等比缩放居中），竖版立绘不再被裁头脚
- **工具栏高亮**（E1，`main_window.py` + `theme.py`）：图片预览/音频/角色按钮 `setCheckable`，新增 `QToolButton:checked` 高亮样式
- **CLI stderr 记录**（C1，`app/core/bundle_parser.py`）：AssetStudio CLI 两处 `DEVNULL` 改为捕获 stderr 并记日志，导出失败能看到原因
- **批量导出防重复**（E4，`export_controller.py`）：加 `_batch_exporting` 标志，导出中再点弹提示，不再叠出多个任务
- **状态栏字号**（A4，`main_window.py` + `theme.py`）：12px → 14px
- **右键菜单命名统一**（D3，`main_window.py`）：统一为「导出 GIF / 导出视频 / 导出合成 GIF / 导出合成视频」，删除空 `pass` 分支

> G5（合成图拉伸变形）暂未修：正确做法是用 SpineViewerCLI `merge` 命令合并导出，属于 Issue #11 未来任务。

### v1.27.0 (2026-08-15)

**Lua 反编译提速 + 反编译并入导入流程 + 调试模式**

- **Lua 反编译提速**（`app/ui/workers/lua_decrypt.py`）：
  - 从「逐文件起 JVM」改为「单 JVM 多线程目录批量反编译」，13757 个文件从十几分钟降到约 1 分钟
  - 核心逻辑抽成模块级函数 `decompile_lua_dir()`，供 `LuaDecryptWorker` 和 `ImportASWorker` 复用
  - `tools/lua/unluac.jar` 替换为批量版（支持 `--threads` + 目录批量）
  - 顺带修复：明文 `.lua.bytes`（约 40 个）不再误判失败；反编译失败的空壳文件写入 `return {}` 占位，不留 `.lua.bytes` 残留
- **反编译并入导入流程**（`app/ui/workers/import_as.py`）：
  - `_stage_export` 导出 TextAsset 后立即反编译，导入完成后 `.lua` 已就绪
- **角色加载免等待**（`app/ui/main_window.py`）：
  - `_start_lua_decrypt` 检查 `.lua` 已就绪则直接加载角色数据，否则回退反编译
- **调试模式**（`main.py` + `debug.bat` + `main_window.py`）：
  - `--debug` 参数启动调试模式，`debug.bat` 一键启动
  - 调试工具栏：选择性导出（全量 / 只导出 Lua / 只导出贴图）+ 清空各数据区域按钮

### v1.26.0 (2026-08-14)

**第二轮重构：main_window.py 垂直拆分（UI视图 / 角色解析 / 导出控制）**

- **新增 `app/ui/views/` 目录**：存放各视图构建函数，返回 `(container, controls_dict)`：
  - `preview_view.py` → `create_preview_view()`（图片预览容器）
  - `audio_view.py` → `create_audio_view()`（音频管理器容器）
  - `character_view.py` → `create_character_view()`（角色数据容器）
  - 主窗口 `_setup_ui` 通过 `self.preview_controls` / `self.audio_controls` / `self.character_controls` 访问控件
- **新增 `app/core/character_loader.py`**：角色 Lua 解析引擎（无 Qt 依赖）。迁移全部 `_parse_*` / `_extract_*` 静态方法及 `ELEMENT_MAP` 常量，`_load_character_data` 委托给模块级函数
- **新增 `app/ui/features/export_controller.py`**：导出流程控制。迁移 `export_composite_video`、`export_with_dialog`、`batch_export_with_dialog` 及全部批量导出槽函数，信号通过 `functools.partial` 直连模块函数，不再依赖 MainWindow 的 `_on_*` 槽方法
- **修复**：`_show_context_menu` 中 `self._is_composite_png` / `self._extract_skin_name_from_png` 改为调用 `spine_adapter` 模块函数
- **清理**：删除 main_window.py 中所有未使用的导入（QGraphicsScene、QPainter、QDialog、BatchExportWorker 等）
- **main_window.py 瘦身至约 2100 行**：仅保留窗口骨架、版本列表、视图容器挂载及既有业务方法

### v1.25.0 (2026-08-14)

**重构 main_window.py 为模块化结构**

- **将 6356 行的 main_window.py 按职责拆分为 5 个子目录、11 个独立文件**：
  - `features/fgui_atlas.py`：FGUI 图集解析（ByteBuffer、UIPackage、UIPackageTool，无 Qt 依赖）
  - `dialogs/image_viewer.py`：大图预览窗口（ImageViewerDialog）
  - `dialogs/export_settings.py`：导出参数设置对话框（ExportSettingsDialog）
  - `widgets/drag_list.py`：支持拖放复制的自定义列表控件（DragListWidget）
  - `workers/image_loader.py`：缩略图异步加载线程（ImageLoadWorker）
  - `workers/preview_export.py`：图片预览导出线程（PreviewExportWorker）
  - `workers/audio_decrypt.py`：音频解密线程（AudioDecryptWorker）
  - `workers/lua_decrypt.py`：Lua 字节码解密线程（LuaDecryptWorker + FIXED_HEAD 常量）
  - `workers/import_as.py`：导入 AS 资源线程（ImportASWorker）
  - `workers/batch_export.py`：批量动画导出线程（BatchExportWorker）
  - `workers/composite_export.py`：批量合成图视频导出线程（CompositeExportWorker）
  - `adapters/spine_adapter.py`：Spine/FFmpeg 工具函数（配对、合成、动画名、视频导出等）
- **main_window.py 瘦身至 ~800 行**：仅保留窗口骨架、视图切换和版本列表逻辑
- **所有静态方法转为模块级函数**：`find_paired_files`、`composite_images`、`get_animation_names` 等
- **新增 `app/core/path_utils.py` 导出 `DATA_DIR` 常量**：供各 Worker 模块统一引用
- **新增 `app/ui/theme.py` 导出 `THUMB_SIZE` 常量**：供 ImageLoadWorker 引用
- **文档更新**：项目架构图补充五子目录结构，新增子目录职责说明表

### v1.24.1 (2026-08-07)

**优化音频文件筛选规则，避免误处理非音频资源**

- **新增筛选规则**（`app/ui/main_window.py` `AudioDecryptWorker._convert_bytes_to_bank`）：
  - 仅处理同时满足以下条件的 `.bytes` 文件：
    - 文件名（不含扩展名）完全由数字组成（`base.isdigit()`）
    - 文件所在路径包含 `fmodassets/` 子目录
  - 不满足条件的文件记录 DEBUG 日志并跳过，不再重命名或复制
  - 日志输出：`"扫描到音频 .bytes 文件: X 个（跳过 Y 个非音频文件）"`
- **新增统一筛选函数 `_should_process_file`**（`tools/epic7_debank_v1_0/epic7_debank.py`）：
  - 封装筛选逻辑（文件名纯数字 + 路径含 fmodassets），提高代码可维护性
  - 应用于 `collect_bank_files` 函数，确保独立运行时也按相同规则过滤
- **文档更新**：在音频管理器流程章节补充说明筛选规则，避免误处理其他资源

### v1.24.0 (2026-08-07)

**音频解密保留目录结构，解决中日文语音文件同名覆盖问题**

- **修复中日文语音文件同名覆盖问题**（`tools/epic7_debank_v1_0/epic7_debank.py`）：
  - 根因：`run()` 函数将所有音频文件平铺复制到 `output/audio/` 根目录，来自 `voice_cn/` 和 `voice_jp/` 的同名文件相互覆盖
  - 重构：将批处理流程改为逐个 bank 文件处理，每个 bank 解密后立即复制
  - 目录结构保留：使用 `os.path.relpath(bank_path, input_root)` 获取相对于输入目录的路径，构建对应的输出子目录
  - 示例：`data/material/fmodassets/voice_cn/xxx.bank` → `output/audio/fmodassets/voice_cn/xxx.wav`
- **子目录级别去重**：在复制每个音频文件时检查目标文件是否存在且大小一致，存在则跳过
- **容错增强**：输出目录创建失败时记录 ERROR 日志并跳过当前 bank 文件；文件复制失败时记录 ERROR 日志并跳过该文件，不中断其他文件处理
- **函数重命名**：`execute_quickbms` → `execute_quickbms_single`，改为处理单个 bank 文件并返回布尔值
- **适配 `_load_audio_list` 递归扫描**（`app/ui/main_window.py`）：
  - 将 `os.listdir` 改为 `os.walk` 递归扫描 `output/audio/` 子目录
  - 文件名显示改为相对于 `output/audio/` 的相对路径，以区分同名文件
- **适配 `_decrypt_bank_files` 去重与计数**（`app/ui/main_window.py`）：
  - 解密前的去重检查和解密后的文件统计均改为 `os.walk` 递归扫描子目录
- **验证**：修改后 `output/audio/` 下生成 `fmodassets/voice_cn/`、`fmodassets/voice_jp/` 等子目录，同名文件分布在不同子目录中，不再相互覆盖

### v1.23.1 (2026-08-07)

**修复 get_base_dir() 路径少一级的 Bug + DATA_DIR 启动日志**

- **修复 `get_base_dir()` 路径计算错误**（`app/core/path_utils.py`）：
  - 根因：`path_utils.py` 位于 `app/core/` 目录下，原代码 `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` 只上溯 2 级到 `app/`，导致 `get_tools_dir()` 返回 `.../app/tools/` 而非 `.../tools/`
  - 修复：增加 1 级 `os.path.dirname()`，上溯 3 级到项目根目录
  - 影响范围：`get_data_dir()`、`get_tools_dir()`、`get_output_dir()`、`get_logs_dir()` 及所有依赖 `get_base_dir()` 的函数全自动修正
- **新增 DATA_DIR 启动日志**（`app/ui/main_window.py`）：
  - 在 `__init__` 中启动时打印 `logger.info(f"当前 DATA_DIR: {DATA_DIR}")`，便于排查路径问题
- **验证**：修正后 `get_tools_dir()` 返回 `C:\Users\...\xl_updata_tool\tools`，`get_data_dir()` 返回 `C:\Users\...\xl_updata_tool\data`，`AssetStudio.CLI.exe` 路径正确

### v1.23.0 (2026-08-07)

**统一 AssetStudio.CLI.exe 路径 + 容错机制**

- **路径统一**：所有 `AssetStudio.CLI.exe` 引用统一使用 `app/core/path_utils.get_tools_dir()` 拼接路径，确保开发环境和打包后路径一致
  - `app/core/bundle_parser.py`：`AS_CLI` 使用 `get_tools_dir()`
  - `app/ui/asset_browser.py`：`AS_CLI` 使用 `get_tools_dir()`
  - `app/ui/main_window.py`：`as_cli` 使用 `get_tools_dir()`
- **新增 `_check_as_cli()` 辅助函数**（`bundle_parser.py`）：
  - 检查 `AssetStudio.CLI.exe` 是否存在，DEBUG 级别记录完整路径
  - 不存在时等待 1 秒后重试 1 次，失败时记录 ERROR 日志并返回空结果（`set()` / `{}`）
  - 替换原有 `raise RuntimeError` 的硬错误方式，确保程序不崩溃
- **容错增强**：
  - `bundle_parser.py`：`subprocess.run()` 增加 `timeout=300` 超时参数，捕获 `TimeoutExpired` 和通用异常，记录日志后返回空结果
  - `asset_browser.py`：`_MapWorker.run()` 和 `_ExtractWorker.run()` 在调用 CLI 前检查文件是否存在，不存在时记录 ERROR 日志并通过信号通知调用方
  - `main_window.py`：`_import_as` 检查 CLI 时添加 DEBUG 日志记录路径；`ImportASWorker._stage_map` 和 `_stage_export` 增加前置文件存在性检查
- **日志记录**：在每次路径拼接和文件检查处添加 DEBUG 日志，记录实际使用的完整路径

### v1.22.0 (2026-08-01)

**打包支持 + 工具路径统一**

- **新增路径适配工具**（`app/core/path_utils.py`）：
  - `get_base_dir()`：返回 exe 所在目录（打包后）或项目根目录（开发时），用于持久化数据
  - `get_tools_dir()`：打包后返回 `sys._MEIPASS/tools`（只读解压资源），开发时返回项目目录的 `tools/`
  - `get_data_dir()` / `get_output_dir()` / `get_logs_dir()`：基于 exe 所在目录，实现绿色便携
- **统一工具路径**：所有 `os.path.dirname(DATA_DIR) + "tools/"` 替换为 `get_tools_dir()`，涉及：
  - `app/ui/main_window.py`：SpineViewerCLI、SpineViewer、AssetStudio CLI、FFmpeg、epic7_debank 路径
  - `app/ui/asset_browser.py`：`AS_CLI` 改用 `get_tools_dir()`
  - `app/core/bundle_parser.py`：`AS_CLI` 改用 `get_tools_dir()`
- **统一输出路径**：所有 `os.path.dirname(DATA_DIR) + "output/"` 替换为 `get_base_dir() + "output/"`
- **新增 `xl_updata_tool.spec` 文件**：PyInstaller `--onedir` 模式配置，含 `tools/` 数据目录和隐藏导入

### v1.21.0 (2026-08-01)

**导入AS 集成到主界面**

- **新增 `ImportASWorker` 工作线程**（`app/ui/main_window.py`）：
  - 整合 `AssetBrowser` 中的 `_FixWorker`、`_MapWorker`、`_auto_export_to_material` 三阶段逻辑
  - 阶段 1（修复文件头）：直接对原始 `.bundle` 文件调用 `fix_bundle_inplace`，不再复制到 `_as_temp`
  - 阶段 2（解析资源）：调用 AssetStudio CLI 生成 `assets_map.json`（`--map_op Both --map_type JSON`）
  - 阶段 3（导出分类）：按类型逐个导出到 `data/material/`（`--group_assets ByContainer`），并清理 `.prefab` 后缀
  - 信号：`progress_stage(str, int, int)` / `stage_finished(str)` / `all_finished(bool, str)`
  - 支持 `cancel()` 取消机制
- **修改 `_import_selected` 方法**：
  - 不再弹出 `AssetBrowser` 窗口，改为启动 `ImportASWorker` 后台线程
  - 复用主界面下载进度条 `dl_progress` 显示当前阶段和进度（如"修复文件头: 150/2956"）
  - 导入按钮在执行期间禁用，完成后恢复
  - 完成后弹窗提示"导入完成！文件已分类到 data/material/"
  - 失败时弹窗提示"文件可能损坏，请点击【删除已下载】并重新下载"
- **保留 `_browse_version` 方法**：标记为备用，当前不再调用
- **资源浏览流程简化**：用户无需等待子窗口弹出，所有进度在主界面状态栏和进度条显示

### v1.20.0 (2026-08-01)

**性能优化 + 日志限制 + 去重**

- **日志系统改造**（`app/core/logger.py`）：
  - 从 `TimedRotatingFileHandler`（按天分割）改为 `RotatingFileHandler`（按大小分割）
  - 固定文件名 `logs/app.log`，单文件最大 10 MB（`maxBytes=10*1024*1024`）
  - `backupCount=0`，超出大小时自动清空重写，仅保留当前文件
  - 文件日志级别 DEBUG，控制台日志级别 INFO
  - 添加 `encoding='utf-8'` 确保中文字符正常
- **窗口卡顿修复**（耗时操作移至后台线程）：
  - 新增 `PreviewExportWorker`：图片预览导出（.skel → PNG）移至 QThread，进度条实时反馈
  - 新增 `AudioDecryptWorker`：音频解密（.bytes → .bank → 解密）移至 QThread
  - 新增 `CompositeExportWorker`：批量合成图视频导出移至 QThread（原主线程串行处理）
  - 所有工作线程支持 `cancel()` 方法，切换视图时自动取消后台任务
  - 信号机制：`progress` / `finished` / `error`，UI 线程通过信号连接更新界面
- **去重优化**（避免重复处理已有文件）：
  - `_as_temp` 复制去重：`_FixWorker` 复制前检查目标文件大小，一致则跳过（`asset_browser.py`）
  - 图片预览导出去重：`PreviewExportWorker` 的 `force=False` 时跳过已存在的 PNG
  - 音频解密去重：`AudioDecryptWorker` 的 `force=False` 时若 output/audio/ 已有音频文件则跳过解密
  - 强制刷新：点击【🔄 重新加载图片】时 `force=True` 重新导出
- **代码重构**：
  - 将 7 个图片导出辅助方法 + 5 个视频导出辅助方法转换为 `@staticmethod`
  - 移除旧的同步方法 `_export_skel_to_png`、`_convert_bytes_to_bank`、`_decrypt_bank_files`（逻辑移入工作线程）
  - `_ffmpeg_composite_videos` 移除 QMessageBox 调用（线程安全）

### v1.19.0 (2026-07-30)

**文档更新**

- 全面更新开发文档指南：
  - 新增「图片预览与导出流程」章节（含动画帧导出、皮肤导出、视频导出流程图）
  - 新增「音频管理器流程」章节（含完整流程图、UI 组件说明、epic7_debank.py 脚本说明）
  - 更新项目架构目录结构（新增 output/、tools/SpineViewer/、tools/epic7_debank_v1_0/）
  - 更新功能特性列表

### v1.18.0 (2026-07-30)

**功能简化 + Bug 修复**

- 音频管理器简化：
  - 移除工具栏【🔓 解密 .bank】按钮（解密改为自动执行）
  - 移除表格"状态"列（所有文件均为已解密）
  - `_load_audio_list` 改为只扫描 `output/audio/` 目录
  - 状态栏移除"已解密"统计
  - 移除所有 `info["status"]` 相关检查
- 音频文件重复问题修复：
  - `epic7_debank.py` 复制文件时强制覆盖（移除冲突检测逻辑）
  - `_decrypt_bank_files` 解密前清理 `output/audio/` 中所有旧音频文件
- 工具栏精简：
  - 移除【预览图片】和【打开SV】按钮
  - 只保留 5 个按钮：检查更新 / 导入AS / 图片预览 / 音频 / 刷新

### v1.17.0 (2026-07-30)

**新功能：音频管理器**

- 工具栏新增【音频】按钮，点击切换到独立音频管理器视图
- 自动解密流程：
  - `_convert_bytes_to_bank`：扫描 `data/material/` 将 .bytes 重命名为 .bank
  - `_decrypt_bank_files`：调用 `epic7_debank.py -i data/material -o output/audio` 解密
  - `_load_audio_list`：扫描 `output/audio/` 加载音频文件列表
- 音频管理器 UI：
  - 表格：复选框 / 文件名 / 时长 / 格式 / 大小
  - 播放控制区：播放/暂停 + 进度条 + 时间显示 + 音量控制
  - 工具栏：刷新列表 / 导出选中 / 播放选中
- 播放器：使用 `QMediaPlayer` + `QAudioOutput`（QtMultimedia）
- 交互：双击播放、右键菜单（打开目录/复制/播放）
- `epic7_debank.py` 脚本重写：
  - 支持 `--input-dir`/`--output-dir` 命令行参数
  - 递归扫描子目录中的 `.bank` 文件
  - 每个 bank 文件独立临时子目录
  - 强制覆盖输出文件

### v1.16.0 (2026-07-30)

**新功能：皮肤（Skin）导出与视频导出集成**

- 皮肤图片导出（`_export_skel_skins`）：
  - 从 `.skel` 二进制文件提取 `motion_*` 名称作为皮肤列表（`_extract_motion_names`）
  - 遍历每个皮肤，使用 SpineViewerCLI 导出独立 PNG
  - 动画固定为 `idle`，添加 `--skins {skin_name}` 参数
  - 输出文件名：`{base_name}_{skin_name}.png`
  - 支持 `--pma` fallback
- 视频导出皮肤参数传递：
  - 新增 `_extract_skin_name_from_png`：从 PNG 文件名提取皮肤名
  - `_export_with_dialog`、`_export_composite_video`、`_export_spine_media_file` 均新增 `skin_name` 参数
  - 皮肤参数只传递给角色视频导出，背景不应用皮肤
  - `BatchExportWorker` entries 从 2 元组改为 3 元组 `(skel, atlas, skin_name)`
- 动画名称提取（`_get_animation_names`）：
  - 三级回退：CLI 查询 → .skel 二进制提取 → 默认 `["idle"]`
  - 正则 `rb'motion_[a-zA-Z0-9_]+'` 搜索二进制数据

### v1.15.0 (2026-07-30)

**新功能：预乘 Alpha 选项 + Bug 修复**

- `ExportSettingsDialog` 新增"预乘 Alpha"复选框（默认勾选）
- 所有导出方法支持 `--pma` 参数：
  - `_export_with_dialog`：根据设置添加 `--pma`
  - `BatchExportWorker.run`：批量导出支持 `--pma`
  - `_export_spine_media_file`：合成图临时视频支持 `--pma`
  - `_export_composite_video`：角色和背景使用相同的 `--pma` 设置
- Bug 修复：批量导出 `AttributeError: 'MainWindow' object has no attribute 'btn_reload'`
  - 将 `_create_preview_view` 中的局部变量 `btn_reload` 改为 `self.btn_reload`
- FFmpeg 路径查找修复：
  - 新增 `_get_ffmpeg_path()` 方法：优先检查 `tools/SpineViewer/ffmpeg.exe`
  - 回退到系统 PATH 中的 `ffmpeg`
  - DEBUG 级别记录实际使用的路径

### v1.14.0 (2026-07-30)

**合成图视频导出方案重构**

- 旧方案（已移除）：PNG 序列合成 + Pillow 逐帧合成 + FFmpeg 编码
  - 问题：大量临时 PNG 文件导致 Windows 文件句柄泄漏，引发 `UnauthorizedAccessException`
- 新方案：SpineViewerCLI 直接导出 MP4 + FFmpeg `filter_complex` 叠加合成
  - 新增 `_export_spine_media_file`：调用 `SpineViewerCLI export -f Mp4` 直接导出单个 MP4
  - 新增 `_ffmpeg_composite_videos`：FFmpeg 叠加角色和背景 MP4
  - 重写 `_export_composite_video`：3 步流程（角色 MP4 → 背景 MP4 → FFmpeg 叠加）
  - 新增 `_export_composite_video_with_params`：不弹窗版本，供批量导出使用
  - 移除旧方法：`_export_png_sequence`、`_composite_frames`、`_encode_mp4`、`_encode_gif`
- FFmpeg colorkey 滤镜修复：
  - 旧滤镜：`format=rgba,colorchannelmixer=aa=1`（假设 MP4 有透明通道，导致黑底覆盖）
  - 新滤镜：`colorkey=0x000000:0.1:0.2`（去除角色视频黑色背景后叠加）
- 批量导出合成图支持：
  - `_show_context_menu` entries 扩展为 3 元组 `(skel, atlas, png_path)`
  - `_batch_export_with_dialog` 检测合成图并调用 `_export_composite_video_with_params`
  - 只弹一次参数窗口，合成图串行处理，普通文件通过 `BatchExportWorker` 处理

### v1.13.0 (2026-07-30)

**功能增强**

- 合成图视频导出功能：
  - 识别合成图：文件名含 `_composite` 后缀的 PNG
  - 合成流程：PNG 序列 → Pillow 逐帧合成 → FFmpeg 编码 MP4
  - 新增方法：
    - `_is_composite_png`: 检测是否为合成图
    - `_find_composite_sources`: 解析角色和背景 skel 路径
    - `_export_composite_video`: 合成图视频导出主流程
    - `_export_png_sequence`: 使用 SpineViewerCLI 导出 PNG 帧序列
    - `_composite_frames`: Pillow `alpha_composite` 逐帧合成
    - `_encode_mp4`: FFmpeg 将 PNG 序列编码为 MP4
    - `_encode_gif`: Pillow 将 PNG 序列编码为 GIF
    - `_cleanup_temp`: 清理临时文件
  - 右键菜单自动切换：
    - 合成图：🎞️ 合成 GIF 预览 / 🎬 合成视频
    - 普通图：🎞️ 导出 GIF 预览 / 🎬 导出为视频
  - 临时文件自动清理（output/temp/）
  - 支持 MP4（FFmpeg）和 GIF（Pillow）两种格式

### v1.12.0 (2026-07-30)

**功能增强**

- 图片预览新增多选支持：
  - 选择模式：`ExtendedSelection`（Ctrl 多选 / Shift 范围选）
  - 右键点击未选中项时自动选中该项
- 图片预览新增拖放支持：
  - 实现 `DragListWidget` 子类，重写 `mimeData`
  - 拖拽文件到桌面/文件夹自动复制
- 右键菜单多选自适应：
  - **单选**：完整菜单（打开目录 / 复制 / 导出GIF / 导出视频）
  - **多选**：简化为批量导出选项
    - 🎞️ 批量导出 GIF (N 个)
    - 🎬 批量导出视频 (N 个)
- 新增 `BatchExportWorker` 批量导出线程：
  - 后台逐个处理，不阻塞 UI
  - 实时进度显示（状态栏）
  - 单个失败不中断，最终汇总报告
- 新增 `_batch_export_with_dialog` 方法：
  - 弹出一次设置对话框，所有文件使用相同参数
  - 确认对话框显示文件数量和参数
  - 导出完成后可自动打开输出目录

### v1.11.0 (2026-07-30)

**功能增强**

- 新增 `ExportSettingsDialog` 导出参数设置对话框：
  - 支持调整：输出格式（MP4/GIF）、动画名称、时长（1-10秒）、帧率（5-60fps）、缩放（1-8x）
  - 实时预览输出文件名
  - 可选"导出后自动打开"
- 右键菜单两个导出项共用同一个对话框，只是默认格式不同：
  - 🎞️ 导出 GIF 预览 → 默认 GIF 格式
  - 🎬 导出为视频 → 默认 MP4 格式
- 统一导出逻辑 `_export_with_dialog`：
  - MP4 输出到 `output/video/`
  - GIF 输出到 `output/character/`
  - 文件名格式：`{skel_name}_{timestamp}.{ext}`
  - 超时时间：60 秒
- 移除旧方法：`_play_animation`、`_export_video_from_skel`、`_get_animation_names` 在 `_play_animation` 中的独立调用、`QInputDialog.getItem` 弹窗选择

### v1.10.2 (2026-07-30)

**功能调整**

- 右键菜单新增【🎬 导出为视频】：
  - 输出目录：`output/video/`（自动创建）
  - 文件命名：`{skel_name}_{timestamp}.mp4`
  - 默认导出 `idle` 动画
  - 导出参数：`--duration 2 --fps 15 --scale 2`
  - 导出完成后自动用系统默认程序打开视频
  - 状态栏实时显示导出进度和结果
  - 超时时间：60 秒

### v1.10.1 (2026-07-30)

**功能调整**

- 图片预览交互变更：
  - 取消左键点击播放动画功能（左键仅选中，不触发动作）
  - 右键菜单新增【🎞️ 导出 GIF 预览】：使用 SpineViewerCLI 导出 GIF 动画并自动用系统默认程序打开
  - 导出参数：`-f Gif --scale 2 --duration 2 --fps 15 --loop`
  - 保留菜单项：📂 打开文件所在目录、📋 复制文件、🎥 导出为视频

### v1.10.0 (2026-07-30)

**功能增强**

- 图片预览视图左键交互：
  - 左键点击缩略图 → 启动 SpineViewerCLI `preview` 命令播放 Spine 动画
  - 预扫描 material 目录建立 skel 路径映射，支持从 PNG 文件名反查 .skel/.atlas
  - 支持多种命名规则匹配：`{name}.png`、`{name}_{anim}.png`、`{name}_bg.png`、`{name}_composite.png`
- 右键菜单扩展：
  - 📂 打开文件所在目录
  - 📋 复制文件
  - 🎬 播放动画预览（新增）
  - 🎥 导出为视频（新增，支持选择动画 + MP4 导出）

### v1.9.0 (2026-07-30)

**功能增强**

- 图片配对合成功能：
  - 自动识别 `xxx.skel` + `xxx_bg.skel` 配对
  - 分别导出角色图和背景图为独立 PNG
  - 使用 Pillow 将角色叠加在背景上生成 `xxx_composite.png`
  - Pillow 未安装时自动跳过合成
- 多动画帧导出：
  - 使用 `SpineViewerCLI query --animations` 获取所有动画名称
  - 遍历导出每个动画的第一帧（`--time 0 --duration 1 --fps 1`）
  - 文件命名：`{name}.png`（idle）、`{name}_{anim}.png`（其他动画）
  - 单个动画导出失败不影响其他动画
- 新增依赖：Pillow>=10.0.0

### v1.8.0 (2026-07-30)

**功能变更**

- 图片预览视图简化：
  - 移除左键点击放大功能（左键点击无反应）
  - 移除复选框/勾选功能
  - 移除全选按钮
  - 移除导出选中按钮
  - 状态栏简化为"共 X 张图片"
- 新增右键菜单功能：
  - 📂 打开文件所在目录：调用 `explorer /select,` 定位并选中文件
  - 📋 复制文件：使用 QMimeData 将文件复制到系统剪贴板
  - 设置 `Qt.CustomContextMenu` 策略，连接 `customContextMenuRequested` 信号

### v1.7.1 (2026-07-30)

**Bug 修复**

- 修复大图预览窗口滚动条问题：
  - 禁用水平/垂直滚动条（ScrollBarAlwaysOff）
  - 初始加载时图片自适应视口（fit-to-view）
  - 窗口大小变化时自动重新适配
  - 用户手动缩放后保持缩放比例不被覆盖
  - 图片居中显示

### v1.7.0 (2026-07-30)

**功能增强**

- 图片预览视图全面升级：
  - 使用 QListWidget (IconMode) 替代固定网格，列数自适应窗口宽度
  - 每个缩略图带复选框，支持多选
  - 新增【☐ 全选/☑ 取消全选】切换按钮
  - 新增【📤 导出选中】按钮，选择目标文件夹后复制选中图片
  - 状态栏显示"已选: X 张 | 共 Y 张图片"
- 大图预览窗口升级：
  - 支持【上一张】/【下一张】导航切换
  - 底部显示当前索引和图片尺寸信息
  - ESC 键关闭窗口
  - 支持 ← → 键盘切换图片
  - 滚轮缩放支持（向上放大，向下缩小）
- 图片缓存逻辑：点击【预览图片】时已有缓存则直接加载

### v1.6.2 (2026-07-30)

**新功能**

- 添加【打开SV】按钮：
  - 位于工具栏【图片预览】与【刷新】之间
  - 点击后独立启动 `tools/SpineViewer/SpineViewer.exe`
  - Windows 下使用 `os.startfile`，其他平台使用 `subprocess.Popen`
  - 文件不存在时记录 ERROR 日志并弹窗提示

### v1.6.1 (2026-07-29)

**功能优化**

- 【预览图片】功能添加缓存检查逻辑：
  - 点击【预览图片】时先检查 `output/character/` 是否已有 PNG 图片
  - 已有图片：跳过导出，直接加载并切换到预览视图（来源：缓存）
  - 没有图片：执行导出流程后切换到预览视图（来源：刚导出）
  - 【🔄 重新加载图片】按钮改为强制重新导出（清空目录 + 重新导出 + 刷新）
  - 状态栏显示图片来源（缓存 / 刚导出）
  - 导出逻辑提取为独立方法 `_export_skel_to_png`

### v1.6.0 (2026-07-29)

**新功能**

- 添加角色图片预览功能：
  - 工具栏新增【图片预览】按钮（位于【刷新】左侧）
  - 点击后切换为独立预览视图，隐藏版本列表
  - 异步加载 `output/character/` 目录中的 PNG 图片（QThread）
  - 网格展示缩略图（每行 5 张，150x150 像素）
  - 加载进度条实时显示
  - 点击缩略图弹出大图预览窗口（支持滚轮缩放）
  - 重新加载按钮刷新网格
  - 右上角【✕ 关闭预览】返回主界面
  - 目录为空时显示"暂无图片"提示

### v1.5.4 (2026-07-29)

**功能优化**

- 【预览图片】功能优化导出参数：
  - 添加 `--scale 4` 参数提高导出图片清晰度
  - 添加 `--max-resolution 8192` 防止画布被压缩
  - 添加 `--pma` 预乘 Alpha 通道支持
  - 实现 `--pma` fallback 逻辑：带 `--pma` 导出失败时自动重试不带 `--pma`
  - 日志记录导出文件大小和 scale 参数

### v1.5.3 (2026-07-29)

**Bug 修复**

- 修复【预览图片】功能的 SpineViewerCLI 命令行参数错误：
  - 原参数格式：`--skeleton`, `--format`, `--output`
  - 修正为：`export <skel> -f Png -o <output> -a idle --atlas <atlas>`
  - 添加默认动画参数 `-a idle`
  - 增加超时时间从 30 秒到 60 秒
  - 添加 stdout/stderr 日志记录（DEBUG 级别）

### v1.5.2 (2026-07-29)

**功能增强**

- 【预览图片】功能添加详细日志记录：
  - 每个文件查找 atlas 时记录 INFO 日志
  - 找到 atlas 时记录 INFO 日志
  - 未找到 atlas 时记录 WARNING 日志（包含查找路径）
  - 便于排查文件匹配问题

### v1.5.1 (2026-07-29)

**Bug 修复**

- 修复【预览图片】功能的 SpineViewerCLI 路径错误：
  - 路径从 `tools/SpineViewerCLI.exe` 修正为 `tools/SpineViewer/SpineViewerCLI.exe`

### v1.5.0 (2026-07-29)

**新功能**

- 添加批量预览图片功能：
  - 在工具栏新增【预览图片】按钮
  - 扫描 `data/material/` 目录中的 `.skel` 文件
  - 查找对应的 `.atlas` 文件
  - 使用 SpineViewerCLI 导出为 PNG 图片
  - 输出到 `output/character/` 目录
  - 支持批量处理，单个文件失败不影响其他文件
  - 显示处理进度和结果统计

### v1.4.9 (2026-07-29)

**Bug 修复**

- 修复 `_cleanup_material_filenames` 函数无法正确扫描 `.prefab` 文件的问题：
  - 改为使用 `os.walk` 递归扫描所有子目录（之前使用 `os.listdir` 只扫描根目录）
  - 添加调试日志：列出目录结构，便于排查问题
  - 添加调试日志：发现 `.prefab` 文件时记录路径
  - 添加 INFO 日志：记录扫描的总文件数
  - 添加 WARNING 日志：路径不是文件时提示

### v1.4.8 (2026-07-29)

**功能增强**

- 添加通用的 `.prefab` 后缀清理功能：
  - 新增 `_cleanup_material_filenames()` 方法，处理所有以 `.prefab` 结尾的文件
  - 遍历 `data/material/` 根目录（不递归子目录），移除文件名末尾的 `.prefab` 后缀
  - 安全检查：重命名前检查目标文件是否已存在，存在则跳过并记录 WARNING 日志
  - 日志记录：每次重命名记录 INFO 日志，便于追踪
  - 单文件失败不影响其他文件处理
  - 替换原 `_fix_atlas_prefab_suffix()` 方法为更通用的实现

### v1.4.7 (2026-07-29)

**功能调整**

- 取消自动导出后的文件分类功能：
  - 移除按扩展名将文件移动到子文件夹（如 `png/`、`json/` 等）的逻辑
  - 所有导出的文件直接保留在 `data/material/` 根目录下
  - 原 `_categorize_material_files()` 方法改为 `_fix_atlas_prefab_suffix()` 方法
  - 保留 `.atlas.prefab` 文件的重命名修复功能（移除错误添加的 `.prefab` 后缀）
  - 更新日志输出，不再记录分类统计

### v1.4.6 (2026-07-29)

**Bug 修复**

- 修复自动分类功能中 `.atlas.prefab` 文件的重命名逻辑：
  - 在 `_categorize_material_files()` 方法中添加对 `.atlas.prefab` 文件的检测和处理
  - 当文件名以 `.atlas.prefab` 结尾时，自动去掉 `.prefab` 后缀，恢复为正确的 `.atlas` 文件
  - `.atlas` 文件分类到 `Atlas` 目录（首字母大写），便于识别
  - 只处理 `.atlas.prefab` 结尾的文件，不影响其他 `.prefab` 文件
  - 日志记录重命名操作（DEBUG 级别）和重命名数量（INFO 级别）

### v1.4.5 (2026-07-29)

**功能增强**

- 自动导出功能支持逐个类型导出：
  - 修改 `_auto_export_to_material()` 方法：从 assets 提取所有唯一类型，初始化类型队列
  - 新增 `_auto_export_next_type()` 方法：从队列中取出下一个类型并调用 CLI 导出
  - 修改 `_run_extract()` 和 `_on_extract_done()` 方法：添加 `auto_export` 参数支持
  - 自动导出流程：逐个类型单独调用 CLI，成功的类型累计文件数，失败的类型记录日志后跳过
  - 所有类型完成后，统计总导出文件数、成功类型数，并执行文件分类
  - 添加详细日志：每个类型导出开始、完成、失败均记录日志

### v1.4.4 (2026-07-29)

**Bug 修复**

- 修复自动导出多类型失败的问题：
  - 将 `_auto_export_to_material()` 方法中的类型列表固定为 `['Texture2D']`
  - 原因：AssetStudio CLI 多类型导出存在兼容性问题，仅单类型 `Texture2D` 导出正常
  - 添加日志说明：`"自动导出仅导出 Texture2D 类型（其他类型暂不兼容）"`
  - 添加代码注释说明原因，便于后续扩展
  - 手动导出功能不变（【导出全部】、【导出筛选】、【导出选中】仍使用完整类型列表）

### v1.4.3 (2026-07-29)

**Bug 修复**

- 修复自动导出功能"导出为空"的问题：
  - 去掉 CLI 的 `--silent` 参数，让 CLI 输出完整日志
  - 新增 `_count_files()` 方法，在导出完成后主动扫描输出目录统计文件数量
  - 改为基于输出目录文件数判断导出成功/失败（不再依赖 CLI 退出码）
  - `result` 信号新增 `file_count` 参数，传递实际导出的文件数量
  - `_on_extract_done()` 方法适配新信号，根据文件数判断是否执行分类
  - 自动创建输出目录（`os.makedirs(self._od, exist_ok=True)`）
  - stderr 输出改为 WARNING 级别（无论退出码是否为 0，都记录 stderr）

### v1.4.2 (2026-07-29)

**Bug 修复**

- 修复 `fix_bundle_inplace` 函数破坏 Bundle 文件结构的问题：
  - 将魔数从 `b"Unity"` 改为 `b"UnityFS"`（正确的 Unity Bundle 文件魔数）
  - 重写 `fix_bundle_inplace` 函数：
    - 搜索 `UnityFS` 魔数定位有效数据
    - 如果文件头已经是 `UnityFS`（偏移量为 0），则不做任何修改
    - 如果找不到 `UnityFS`，跳过并记录 WARNING 日志
    - 添加文件大小检查（小于 8 字节时跳过）
    - 添加 try-except 容错，确保单文件失败不中断流程
  - 重写 `needs_fix` 函数，检查文件头是否为 `UnityFS`
  - 添加详细日志记录（DEBUG/INFO/WARNING/ERROR 级别）

### v1.4.1 (2026-07-29)

**Bug 修复**

- 修复自动导出功能"导出为空但没有报错"的问题：
  - 增强 `_ExtractWorker.run()` 方法的日志输出：
    - 记录 CLI 完整命令行（DEBUG 级别）
    - 记录 CLI 的 stdout 输出前 500 字符（DEBUG 级别）
    - 记录 CLI 的 stderr 输出前 500 字符（ERROR 级别，仅非零退出码时）
    - 记录 CLI 退出码值（INFO 级别）
  - 增强 `_run_extract()` 方法的前置检查：
    - 检查 types 列表是否为空，为空时记录 ERROR 日志并弹窗提示
    - 检查 bundle_dir 是否存在且不为空，无效时记录 ERROR 日志并弹窗提示
  - 增强 `_on_extract_done()` 方法的错误处理：
    - 手动导出失败时弹窗提示具体错误原因
    - 自动导出完成后检查文件数量，为 0 时记录 ERROR 日志
    - 记录导出文件数量，方便排查问题
  - 增加超时异常处理：CLI 执行超过 300 秒时记录 ERROR 日志

### v1.4.0 (2026-07-29)

**功能增强**

- 导入AS后自动导出资源到 `data/material/` 目录：
  - 新增 `_auto_export_to_material()` 方法，在资源解析完成后自动触发导出
  - 新增 `_categorize_material_files()` 方法，导出完成后按文件扩展名自动分类存放
  - 导出目录 `data/material/` 按类型创建子文件夹（如 `txt/`、`json/`、`prefab/`、`bytes/`、`asset/` 等）
  - 静默执行模式：后台执行，不弹窗，通过日志记录执行状态
  - 容错机制：单个文件移动失败时记录日志并跳过，不中断整体流程
  - 保持原有手动导出功能不变（【导出全部】、【导出筛选】、【导出选中】按钮仍正常工作，导出到 `data/extracted/`）

### v1.3.0 (2026-07-28)

**功能增强**

- 资源浏览器类型筛选支持多选：
  - 新增 `_MultiSelectComboBox` 自定义按钮类（基于 `QPushButton`），支持复选框多选
  - 新增 `_TypePopup` 弹出窗口类，包含带复选框的类型列表
  - 下拉菜单中每个类型前显示复选框，用户可勾选多个类型
  - 控件显示"全部类型"（未选择时）、类型名称（选择1个时）或"已选 N 种类型"（选择多个时）
  - 交互逻辑：点击复选框或选项文字切换选中状态，下拉列表保持打开，点击外部自动关闭
  - 表格筛选逻辑更新：只显示所选类型的资源，未选择任何类型时显示全部资源
  - 类型筛选操作记录到日志（INFO 级别），记录选中的类型列表

### v1.2.0 (2026-07-28)

**功能增强**

- 重构"导入AS"功能执行流程，实现即时反馈：
  - 点击"导入AS"后立即打开资源浏览器窗口
  - 在窗口顶部显示"修复文件头: 已完成 X/XXX"进度条，实时更新
  - 文件修复完成后自动启动 AssetStudio 解析资源
  - 解析完成后表格显示资源列表

- 新增 `_FixWorker` 线程类：
  - 异步处理文件复制和修复操作
  - 使用 Qt 信号/槽机制更新 UI，确保线程安全
  - 单个文件修复失败时记录日志并跳过，继续处理后续文件
  - 所有文件修复失败时弹出提示并关闭窗口

**Bug 修复**

- 修复资源浏览器初始化流程，分离文件修复和资源解析阶段

**日志增强**

- 记录每次导入操作的开始、每个文件的修复状态、最终成功/失败信息
- 在 `_FixWorker` 中添加详细的日志记录

### v1.1.0 (2026-07-28)

**Bug 修复**

- 修复 `_dl_done` 命名冲突问题：将进度计数变量重命名为 `_dl_count`，避免与同名方法冲突
- 原问题：`TypeError: 'int' object is not callable`

**新增功能**

- 添加日志系统：`app/core/logger.py`，支持按天分割日志文件
- 在 `main.py` 和 `main_window.py` 中集成日志记录
- 为下载相关操作添加 try-except 容错机制
- 连接 `item_fail` 信号，记录下载失败日志

**改进**

- 下载任务遇到错误时记录日志并跳过，继续执行后续任务
- 数据库更新操作添加异常捕获，避免单个文件失败导致程序崩溃

---

## 常见问题

**Q: 点击【角色】后报错 `TypeError: missing 1 required positional argument: 'self'`**

A: 这是第二轮重构时调用残留导致的。`_load_character_data` 已恢复为 `MainWindow` 的实例方法，请更新到最新代码。如仍遇到，检查 `main_window.py` 中 `_load_character_data` 是否包含 `self` 参数。

---

## 附录 A：项目关键文件清单

### A.1 核心调用层（CLI 和解析逻辑）

| 文件路径 | 预期作用 | subprocess 调用 | 详细说明 |
|----------|----------|----------------|----------|
| `app/core/bundle_parser.py` | Bundle 解析、调用 CLI 提取 hash 清单 | ✅ `subprocess.run()` | 在 `extract_manifest_hashes()` 和 `extract_manifest_from_dir()` 中调用 CLI，使用 `cwd=os.path.dirname(AS_CLI)`，但 `stderr=subprocess.DEVNULL` 丢弃了错误输出 |
| `app/ui/asset_browser.py` | 资源浏览器窗口，后台线程生成资源映射 | ✅ `subprocess.Popen()` 和 `subprocess.run()` | `_MapWorker` 使用 `Popen`，`_ExtractWorker` 使用 `run`，均设置了 `cwd=os.path.dirname(AS_CLI)` |
| `app/core/downloader.py` | 下载器，检查更新 | ❌ 无 | 不涉及 CLI 调用，仅处理 HTTP 请求和文件下载 |

**CLI 调用代码片段**

`bundle_parser.py` 第42-51行：
```python
subprocess.run([
    AS_CLI, category_path, out_dir,
    "--game", "UnityCN", "--key_index", "23",
    "--group_assets", "ByType",
    "--export_type", "Convert",
    "--silent",
], cwd=os.path.dirname(AS_CLI),
    stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
    creationflags=subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0,
)
```

`asset_browser.py` `_MapWorker` 类（使用 Popen）：
```python
proc = subprocess.Popen(
    [AS_CLI, self._bd, md, "--game", "UnityCN", "--key_index", "23",
     "--map_op", "Both", "--map_type", "JSON", "--silent"],
    cwd=os.path.dirname(AS_CLI),
    stdout=subprocess.PIPE, stderr=subprocess.PIPE,
    text=True, bufsize=1,
    creationflags=subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0)
```

### A.2 运行时依赖（CLI 运行环境）

| 文件/目录 | 预期作用 | 状态 |
|-----------|----------|------|
| `tools/AssetStudio/AssetStudio.CLI.exe` | CLI 主程序（路径通过 `get_tools_dir()` 解析） | ✅ 存在 |
| `tools/AssetStudio/Keys.json` | 密钥配置（用于解密 Unity 资源） | ✅ 存在 |
| `tools/AssetStudio/AI/versions.json` | AI 版本索引 | ✅ 存在 |
| `tools/AssetStudio/Maps/` | 资源映射缓存输出目录 | ✅ 存在（包含 assets_map.bin） |

### A.3 临时数据与日志（运行状态）

| 文件/目录 | 预期作用 | 状态 |
|-----------|----------|------|
| `data/_as_temp/` | 临时存放修复后的 Bundle 文件 | ✅ 存在 |
| `logs/` | 程序运行日志目录 | ✅ 存在 |
| `logs/app_YYYYMMDD.log` | 当日运行日志文件 | ✅ 按天生成 |

### A.4 界面入口（调用发起方）

| 文件路径 | 关键函数 | 传递参数 |
|----------|----------|----------|
| `app/ui/main_window.py` | `_import_selected()` → `_browse_version(ts)` | 传递文件列表 `fs` 和版本时间戳 `ts` |
| `app/ui/main_window.py` | `_browse_version(ts)` | 调用 `AssetBrowser(self, fs, ts)`，**不传递** `_as_temp` 路径 |
| `app/ui/main_window.py` | `_load_character_data()` 实例方法 | UI 协调层，内部调用 `app.core.character_loader.load_character_data` |
| `app/core/character_loader.py` | 纯解析引擎（模块级函数） | 无 Qt 依赖，所有 `_parse_*`/`_extract_*` 函数及 `load_character_data` 集中于此 |

**说明**：`_as_temp` 目录是在 `AssetBrowser` 内部创建的，位于 `data/_as_temp/`。

### A.5 当前问题与改进建议

**问题 1：stderr 未捕获**

- `bundle_parser.py` 中 `stderr=subprocess.DEVNULL`，CLI 报错信息被完全丢弃
- `asset_browser.py` 的 `_MapWorker` 虽然设置了 `stderr=subprocess.PIPE`，但错误时只显示 "Map generation failed"，没有输出 stderr 内容

**改进建议**：
1. 在 `bundle_parser.py` 中捕获 stderr 并记录到日志
2. 在 `asset_browser.py` 的 `_MapWorker` 中，当 map 生成失败时，输出 stderr 内容到日志

**问题 2：文件完整性校验缺失**

- 当前代码在调用 CLI 前未校验文件数量是否与预期一致
- 如果部分文件下载失败，CLI 可能因缺少依赖而报错

**改进建议**：在调用 CLI 前检查 `_as_temp` 目录中的文件数量是否与预期一致，不一致时给出警告。

---

## 附录 B：性能排查清单

当程序出现卡顿或性能问题时，按以下清单逐项确认信息，以便定位瓶颈。

### B.1 需确认的信息

| 编号 | 确认项 | 说明 | 排查方法 |
|------|--------|------|----------|
| 1 | `data/bundles/` 目录下版本文件夹数量 | 文件夹数量过多会导致版本列表加载变慢 | 资源管理器中查看 `data/bundles/` 下的子目录数量 |
| 2 | `output/character/` 目录中图片数量 | 图片数量过多会导致预览视图加载卡顿（缩略图生成、网格渲染） | 资源管理器中查看 `output/character/` 下的 `.png` 文件数量 |
| 3 | 卡顿发生时机 | 首次点击 = 可能是导出耗时；每次点击 = 可能是渲染或内存问题 | 分别记录首次点击和后续点击的响应时间 |
| 4 | 日志文件大小 | 日志过大可能影响写入性能 | 查看 `logs/app.log` 文件大小；当前日志系统已使用 `RotatingFileHandler` 限制单文件 10MB，`backupCount=0` 超出时自动清空重写 |
| 5 | 电脑配置 | CPU 核心数、内存容量、硬盘类型（SSD/HDD）直接影响 I/O 密集型操作 | 任务管理器 → 性能 标签页查看 |
| 6 | 进程资源占用 | 卡顿时 Python 进程的 CPU/内存占用情况可帮助判断瓶颈类型 | 任务管理器 → 详细信息 → 找到 `python.exe`，查看 CPU 和内存列 |

### B.2 性能瓶颈分析指引

> **v1.20.0 已实施优化**：以下耗时操作均已移至 QThread 后台线程，UI 不会阻塞：
> - 图片预览导出 → `PreviewExportWorker`
> - 音频解密 → `AudioDecryptWorker`
> - 批量合成图视频导出 → `CompositeExportWorker`
> - 批量普通文件导出 → `BatchExportWorker`
> - `_as_temp` 复制 + 修复 → `_FixWorker`
>
> 若卡顿仍然出现，应优先检查是否在 UI 线程中执行了新的同步操作。

根据上述信息，可初步判断瓶颈类型：

| 症状 | 可能原因 | 优化方向 |
|------|----------|----------|
| 首次点击卡顿，后续正常 | SpineViewerCLI 导出耗时（I/O 密集） | 检查 `.skel` 文件数量和大小；确认硬盘为 SSD |
| 每次点击都卡顿 | 缩略图渲染或内存不足 | 检查 `output/character/` 图片数量；考虑分页加载 |
| CPU 占用 100% | SpineViewerCLI 或 FFmpeg 子进程满载 | 属正常现象，等待子进程完成即可 |
| 内存占用持续增长 | 图片缓存未释放或 `QPixmap` 泄漏 | 检查 `DragListWidget` 的 `QListWidgetItem` 是否正确回收 |
| 硬盘 I/O 等待高 | 大量小文件读写（如 .bank 解密、PNG 导出） | 确认硬盘类型；HDD 下小文件 I/O 性能较差 |

### B.3 日志系统现状

当前日志系统（`app/core/logger.py`，v1.20.0 已优化）配置：

- **日志分割方式**：按大小分割（`RotatingFileHandler`），固定文件名 `logs/app.log`
- **maxBytes 限制**：✅ 10 MB（`maxBytes=10*1024*1024`）
- **backupCount 限制**：✅ 0（仅保留当前文件，超出大小时自动清空重写）
- **编码**：`utf-8`（支持中文字符）
- **日志级别**：DEBUG（文件）/ INFO（控制台）
- **配置代码**：
  ```python
  from logging.handlers import RotatingFileHandler
  file_handler = RotatingFileHandler(
      log_file,
      maxBytes=10 * 1024 * 1024,
      backupCount=0,
      encoding="utf-8"
  )
  ```
