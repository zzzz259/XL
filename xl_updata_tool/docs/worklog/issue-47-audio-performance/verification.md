# Issue #47 验证记录

## 计划门禁

- 音频树勾选、目录递归和 Ctrl 多选 Qt 测试。
- 7000+ 文件目录索引与首层构造性能基准。
- 全量 pytest、Ruff、AST/编译检查、Qt 启动冒烟。
- `git diff --check` 和未读/导出行为回归。

## 已完成检查

- 全量 `pytest -q`：通过。
- 音频、UI、音频库和音频仓库定向测试：通过。
- `ruff check --no-cache app tests`：通过。
- AST 检查：`AST_OK 149`。
- Qt 启动冒烟：`SMOKE_OK 9.084 True 0 ['album', 'voice']`；预热完成后首层只显示两个根目录。
- 性能基准：7211 个音频文件，目录索引约 1.229 秒，根节点构造约 0.003 秒，首次展开仅构造当前目录子层。
- `git diff --check`：通过；Git 换行提示及 `.pytest_cache` 权限警告不属于代码失败。

## 用户验收项

- [ ] 从版本列表启动，确认版本列表可以立即操作，后台预热不抢占界面。
- [ ] 进入音频页，确认首层只有 `album`、`voice`，展开后再逐层出现目录和文件。
- [ ] 反复点击复选框指示器、行文本，确认勾选和取消都稳定生效。
- [ ] 使用 Ctrl 多选、目录勾选和半选状态，确认选择范围与状态栏数量一致。
