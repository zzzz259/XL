# 安全与凭据

## 凭据处理

- 不提交 QQ bot 凭据、B 站 `SESSDATA`、访问令牌、私钥或生产配置。将本地 `config.toml` 从受版本控制的 `config.toml.example` 复制生成，并限制文件访问权限。
- 不把密钥放进源码、Issue、PR、截图、命令行参数或公开日志。分享日志前清除 token、cookie、用户标识、个人路径及原始游戏资源。
- 怀疑凭据泄漏时，按已泄漏处理：立即撤销/轮换，检查访问记录并清理暴露位置；仅删除公开副本不足以恢复安全。
- CI 使用仓库 Secret，权限遵循最小授权；不在 fork PR 中暴露秘密。

## 文件与外部进程

- 下载的 bundle、工具中间产物、数据库、发送队列、运行日志和导出文件属于运行数据，不应提交。
- 对外部工具输入做路径范围校验；保留已有可用产物直到新产物验证成功；外部进程使用参数数组而非 shell 拼接，并记录足够的失败阶段信息。
- 原始工具日志可能包含其他机器的绝对路径。不要将其当作当前机器诊断证据或未经审查地公开。

## 报告安全问题

请勿在公开 Issue 中贴凭据或可复用的资源样本。若仓库启用了 GitHub Private Vulnerability Reporting，请使用该入口；否则通过仓库维护者的私下渠道报告，并提供最小复现和影响范围。
