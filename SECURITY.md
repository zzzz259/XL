# 安全与凭据约定

## 凭据

- 禁止把 GitHub Token、密码、私钥或服务凭据写入源码、Issue、PR、聊天记录、脚本或命令参数。
- GitHub 操作使用 Git Credential Manager、已认证的 GitHub CLI 或受控插件；Token 使用最小权限并定期轮换。
- 如果凭据曾经出现在日志、聊天记录或命令历史中，按已泄露处理，立即撤销并重新生成。

## 外部资源与工具

- 下载的 bundle、AssetStudio 中间产物和运行时输出不应提交到 Git。
- 外部 CLI 的路径必须通过统一路径服务解析，不应散落硬编码。
- 启动外部程序时优先使用参数列表并关闭 shell 解释，避免把路径交给 shell 重新解析。
- 解析外部资源时应限制输入目录、记录失败阶段，并避免在失败前删除已有可用输出。

## 报告问题

不要在公开 Issue 中粘贴 Token、完整日志中的敏感路径或原始资源。报告安全问题时，先通知维护者并提供最小复现信息。
