# 开发计划与沟通事项

> 本文档用于双方对齐开发方向。每条问题写清「现状 / 期望 / 用户感知」，方便逐条提 Issue 或直接沟通。
> 协作规范见 [CONTRIBUTING.md](CONTRIBUTING.md)。
> 所有行号对应 `app/ui/main_window.py`（6356 行），随重构可能变化，以方法名为准。

## 当前架构收口状态（2026-08）

- 已建立 Issue #21，作为稳定性、架构边界和多人协作基线。
- `app/core/downloader.py` 已去除 Qt 线程依赖，Qt 包装迁移到 `app/ui/workers/download.py`。
- 已加入 pytest、ruff、全量 compile smoke、Windows CI 和核心层无 Qt 回归测试。
- 已补充 [架构与协作基线](xl_updata_tool/docs/架构与协作基线.md)，后续以“先稳定契约、再按领域拆分”为原则。
- 导入 AS 已改为 staging → 分类事务提交；任一分类失败/取消时清理临时目录并保留旧 `data/material` 产物。
- 旧 AssetBrowser 已集中到 `app/ui/legacy/asset_browser_entry.py` 兼容入口；磁盘 Bundle 状态同步已从 MainWindow 下沉到 `app/core/local_bundle_sync.py`。
- 角色缓存读写、源文件失效判断和表格索引派生已下沉到 `app/core/character_cache.py`，MainWindow 保留 UI 协调。
- 角色详情 HTML 与 CSV 文件生成已下沉到 `app/core/character_presenter.py`，MainWindow 只处理 Qt 控件和文件选择对话框。
- 音频目录扫描与预览图 Spine 路径匹配已下沉到 `app/core/audio_library.py`、`app/core/preview_catalog.py`。
- 内置版本种子注册已下沉到 `app/core/seed_versions.py`，启动窗口不再持有网络/清单注册细节。
- 已下载版本文件删除与数据库状态清理已下沉到 `app/core/version_cleanup.py`。
- 版本检查结果注册、下载状态回写和导入变更日志已下沉到 `app/core/version_update.py`。
- UI 领域拆分已新增 `app/ui/features/audio_controller.py`、`app/ui/features/preview_controller.py`，MainWindow 只保留 worker、进度和 Qt 状态协调。
- Ruff 已完成 UI 低风险历史清理，当前 `xl_updata_tool/app/ui`、`xl_updata_tool/app/core`、`xl_updata_tool/tests` 全部通过；CI 强制检查新增版本视图和兼容入口。
- 测试、`pyproject.toml`、运行依赖和开发依赖均已归属到 `xl_updata_tool/`，CI 从项目目录统一执行。
- 已完成 PR 前收口：工作区保持干净，测试、编译、导入检查和人工启动/核心导入验证均已完成；本轮变更将在配置归属调整完成后提交新的 PR。
- `main_window.py`、`character_loader.py`、`asset_browser.py` 等大文件仍需渐进拆分；本轮不以一次性重写为目标。

### Issue #38：日志可观测性与 Normal/Debug 运行模式（2026-08-23）

- 根因已确认：旧 logger 在模块导入时固定为 DEBUG 文件/INFO 控制台，`--debug` 在主窗口导入后才解析，运行模式没有真正参与日志配置。
- 已建立 `RuntimeConfig`，启动顺序调整为“解析参数 → 配置日志 → 注册异常报告 → 导入应用模块”。
- 已新增会话日志目录、普通/Debug 日志 profile、task/component/stage 上下文、环境摘要、崩溃报告和外部进程诊断包装。
- 已接入导入、Lua、音频 Worker 的任务日志，以及 AssetStudio manifest 和 Spine CLI 的过程日志；未改变导出结果、取消语义和 UI 布局。
- 后续待处理：Debug 数据目录隔离、失败 staging 保留、Debug 永久视觉标识和更多流式外部工具调用点的统一收口。

### 项目结构与多人协作架构审查（2026-08-23，评估阶段）

- 当前主要协作风险不是单个文件过长，而是 `main_window.py`、`app/core/`、部分 Worker 同时承担多个领域的编排和状态转换。
- 本轮先做职责地图、依赖热点、提交历史、测试边界和运行契约审查，不立即移动目录或重写公共文件。
- 目标是将主窗口收口为壳层，将领域流程下沉为应用服务，将 Worker 收口为 Qt 生命周期适配器，并为版本/导入/Lua/角色/音频/立绘/FGUI/诊断建立稳定协作边界。
- 迁移必须按小阶段进行：先加兼容入口和契约测试，再迁移调用方，最后删除旧实现；每阶段保持可运行、可回滚和可独立提交。
- 审查结果：`main_window.py` 约 2331 行，在 73 个项目提交的文件历史中出现 27 次；它同时承担窗口壳、版本/导入/音频/Lua/预览流程和多个基础设施调用，是当前首要公共冲突点。
- 结构性根因：项目缺少独立的应用服务/用例编排层，`core`、`features` 和 Worker 仍分别混入基础设施、Qt 生命周期、外部工具和业务副作用。
- 目标路线：先建立 ownership 和依赖门禁，再抽版本/下载与 AS 导入应用服务，随后薄化 Worker、收口外部工具，最后按领域拆分 core/UI 并隔离旧 AssetBrowser；全程保持 output、data、logs、数据库和 Qt 信号契约。
- 本轮记录已同步至 `xl_updata_tool/docs/worklog/findings.md`、`task_plan.md`、`progress.md`；等待用户确认后再进入结构调整实施阶段。
- 已追加第三方所有权审查复核报告：`xl_updata_tool/docs/worklog/ownership-review-2026-08-23.md`。报告逐条核验 17 个要点，并将 Feature-first、Page 自有控件、Composition Root、Worker 薄化、后处理注册表、parser 拆分、依赖门禁、Change Fragment 和分阶段迁移纳入完整计划。
- 计划采用 P0 架构门禁 → Audio → Characters → Versions → Importer → Preview → Platform/Shared 收缩 → 文档降冲突 → ownership 验收的顺序；不进行一次性全仓搬迁。
- 2026-08-24 已创建 Issue #39《架构迁移 P0：建立 Feature ownership 与依赖门禁》，当前只执行无行为变化的契约、装配入口、架构测试和 ownership 文档基线。
- P0 已完成并经用户正常启动验证；2026-08-24 创建 Issue #40《架构迁移 P1：Audio Feature ownership 模板》，开始按 Audio → Characters → Versions → Importer → Preview 顺序迁移。

### 界面视觉重设计（Issue #27，进行中）

- 设计方向已确认：将 XL 定位为“专业资源工作台 + 克制的游戏资产感”，蓝灰深色为唯一正式主题，其他主题仅保留兼容迁移能力。
- 已完成第一阶段主题与主窗口壳层收口，用户已正常启动验证通过：顶部品牌、侧边操作、页面导航和主题入口均正常，无明显布局异常。
- 当前第二阶段正在迁移页面骨架：新增 `app/ui/widgets/view_chrome.py`，统一图片、音频、角色页的页头、操作栏、状态和空状态；版本列表增加工作区摘要并整体控制显隐。
- 本阶段明确不改更新、导入、下载、版本选择、后台 worker、数据目录和数据库契约；页面控制器继续通过现有 MainWindow 回调工作。
- 本次代码验证：内存编译 59 个 Python 文件、Ruff（`app/ui` 与 `tests`）通过；专用可写临时目录下 35 项测试通过；页面构建截图通过。等待用户对第二阶段正常启动和页面切换进行验证后继续下一轮 UI 细化。
- 根据用户反馈修正图片预览空状态：从底部独立黑色横条改为内容区中央叠加文字；角色页同类空状态同步修正，并增加 UI smoke 约束。
- 根据用户实机截图修正页面间黑色间隙：`viewContainer`/`viewContent` 统一使用 `BG_DARK`，保留间距但不再显示 Qt 默认黑色；三页离屏回归和 35 项测试通过。
- 角色页详情区进入下一轮优化：未选中角色时在详情面板中央显示上下文提示，选中后显示原有角色详情；刷新或无选择时不保留过期详情。等待用户验证真实选中流程。
- 用户已验证角色 Wiki 详情、技能数字高亮、角色切换和刷新流程正常；Issue #28 首版进入稳定状态。

### 角色 Wiki 详情页（Issue #27 后续子计划，已纳入）

- 当前详情展示的根因问题已确认：`build_character_detail_html()` 将基础信息、战斗属性、技能、消耗、语音和故事拼成一段 HTML，再放进单个 QLabel，信息完整但不可扫描、不可分区、缺少 Wiki 层级。
- 计划改为“纯 Python 结构化资料模型 + Qt 原生 Wiki 分区组件”：身份头部、核心属性网格、技能卡片/分区、成长消耗表、语音与故事折叠区。
- 第一阶段复用现有角色字段，不修改 Lua 解析、缓存格式和 CSV 契约；头像、立绘关联、关系图谱等内容后置，只有在资源映射可靠时再接入。
- 协作边界：`app/core/` 负责资料分组与缺失值策略，`app/ui/widgets/` 负责 Wiki 组件，`character_view.py` 只负责组装，`MainWindow` 只负责选择和传递资料。
- 验收将覆盖 101 个角色、切换/搜索/刷新、长技能文本、多行故事、缺失字段和 CSV 导出回归。

### 其他页面视觉收口（Issue #29，进行中）

- 已登记图片预览、音频与版本工作区的独立收口范围，明确不修改导出、解密、下载和数据契约。
- 图片预览新增总数/选择数反馈，缩略图与进度条样式纳入主题令牌；音频页增加内容区空状态，播放反馈改为稳定中文文本并清理 emoji；版本表格迁移到统一 `workspaceTable` 样式。
- 本轮静态检查、80 个 Python 文件编译、37 项全量测试和三页离屏构建均通过，等待用户正常启动验证。
- 用户已确认 Issue #29 图片预览、音频和版本工作区改动正常。

### 对话框与主窗口局部视觉收口（Issue #30，进行中）

- 已登记图片查看器、角色选择、导出设置和右键菜单的统一视觉范围，明确不改查看、选择、导出和调试动作契约。
- 已完成对象名与主题令牌迁移，图片查看器空列表导航状态补齐；右键菜单文本去除 emoji，保留原菜单动作。
- 已完成 Ruff、针对性对话框测试和三类对话框离屏截图验证，等待用户正常启动验证。

### 本轮架构稳定性与协作收口（已完成，待 review）

- **稳定性**：AssetStudio 导入采用 staging → 校验 → 提交的事务边界；部分失败、取消和重复执行时保留已有有效产物。
- **架构**：版本、下载、角色、音频、预览和本地 Bundle 状态等逻辑逐步从 `MainWindow` 下沉到 `app/core/` 与 `app/ui/features/`；旧版 AssetBrowser 通过兼容入口隔离。
- **质量门槛**：新增 pytest、Ruff、全量 Python compile smoke、导入 smoke 和 Windows CI；核心层保持无 Qt 依赖。
- **文档**：同步《架构与协作基线》《开发文档指南》、本计划和 `xl_updata_tool/README.md`，明确输出目录契约、协作边界和验收清单。
- **验证**：33 项测试断言通过，`xl_updata_tool/app/` 与 `xl_updata_tool/tests/` 下 75 个 Python 文件内存编译通过，核心入口导入通过，并完成正常启动、AssetStudio 导入和 Lua 导出人工验证。

### 测试目录归属调整（已完成，Issue #23，纳入本轮后续 PR）

- 将根目录 `tests/` 迁移到 `xl_updata_tool/tests/`，避免多工具仓库根目录与具体项目测试边界混淆。
- 同步 pytest `testpaths`、Ruff、README、CI、compile smoke、依赖安装方式和项目内部相对路径。
- 不改变测试断言、生产代码和运行时目录；迁移后 33 项测试断言仍全部通过。

### Python 项目配置归属调整（已完成，Issue #25，纳入本轮后续 PR）

- 将根目录 `pyproject.toml`、`requirements-dev.txt` 迁移到 `xl_updata_tool/`，使项目配置与源代码、测试和运行依赖保持同一边界。
- `requirements.txt` 继续只承载运行时依赖；`requirements-dev.txt` 通过 `-r requirements.txt` 复用运行时依赖，并增加 pytest/Ruff。
- CI 改为从 `xl_updata_tool/` 执行安装、测试、Ruff 和无写盘编译检查。

---

## 一、已完成但需告知的事项

### 1. P0 仓库卫生清理（已完成，已合并 PR #1）

- 新增 `.gitignore`，从索引移除 13,829 个误提交文件（`.pyc`/测试输入/运行时数据）
- 删除冗余 `app/data/` 目录
- **保留**：`data/xl_updata.db`（6 版本 JSON + 74596 sub_bundle）完整未动，功能验证 OK

### 2. 项目结构规范化（已完成，已合并 PR #1）

- README.txt → README.md，仓库根新增 README.md（总介绍 + 项目导航）
- 打包配置统一命名 `build.spec`（删旧 `main.spec`、重命名 `xl_updata_tool.spec`）
- 开发文档移入 `docs/`
- 新增 `项目规范.md`（结构 / 代码 / 命名规范）
- `.gitignore` 补 `epic7_debank_v1_0/_tempo/`，移除 17 个误跟踪 .fsb

### 3. 新增 CONTRIBUTING.md + 本计划文档

协作规范和开发计划，请过目。

### 4. P2 UX 改进第二批 + 可配置导出 + 音频分类（已完成，已合并 PR #15）

> 开发分支 `feat/p2-bg-export`，对应 **Issue #13**。详见 `xl_updata_tool/docs/版本历史.md` v1.29.0。

**（a）P2 UX 第二批（`版本历史.md` v1.29.0 第 3–7、9 项）**：
- B2 视图切换不再杀后台任务（任务与视图解耦）
- A1/A2 导入AS、音频解密新增非模态进度弹窗
- F1 角色立绘按编号分目录（`output/character/<角色ID>/`）
- F2 FGUI 图集切割分目录（`output/fgui/<包名>/`）
- 工具栏拆分：顶部视图栏 + 侧边功能栏，视图按钮高亮
- 清空 ab 包后同步数据库下载状态

**（b）导入AS 可配置导出分类（Issue #13）**：侧边栏 4 类勾选（lua / 角色立绘 / FGUI图集 / 音频），按 Container 前缀严格区分（`EXPORT_CATEGORIES` 常量），不再全量导出。

**（c）音频分类导出（Issue #13）**：语音按角色分目录（`voice/<ID>/<cn|jp>`），bgm 按专辑分目录（`album/<专辑名>`），复用反编译 lua 构建专辑映射（新增 `app/core/album_map.py`）。

**（d）版本备注 delta 显示**：版本列表备注列显示「新增 X | 移除 Y | 未变 Z」，替代种子版本硬编码文案。

### 5. 导入AS 进度反馈 + 分类清空 + 状态同步 + 立绘精确筛选 + 播放器 bug（已完成，已合并 PR #15）

> 开发分支 `feat/p2-bg-export`，对应 **Issue #14**。详见 `xl_updata_tool/docs/版本历史.md` v1.31.0。

- 修复点「音频」崩 `_audio_player` AttributeError（`__init__` 未初始化），音频 debank 恢复。
- `_stage_export` 从清空整个 material 改为只清空本次勾选分类目录，二次导出不再清掉 lua 反编译产物。
- 新增 `_sync_local_bundles()`：扫描磁盘实际 `.bundle` 同步 DB 下载状态（磁盘为准），修复「磁盘有全量 12797 个但 app 只显示 368 个」的状态脱节。
- 导出/反编译进度：解析 CLI `Loading` 行逐文件跳动 + 反编译逐文件进度，不再只显示「0/1」。
- 角色立绘精确化：`cardspine` + `battlespine_1\d{4}`，排除怪物/npc/UI/小游戏/光影。

### 6. debug 全量/增量导入 + 侧边栏 2×2 + 排除 _e/_n（已完成，已合并 PR #15）

> 对应 **Issue #16**（+ 关联 Issue #14 角色立绘筛选延伸）。详见 `xl_updata_tool/docs/版本历史.md` v1.32.0。

- debug 工具栏加「导入范围」下拉框（全量导入 / 增量导入），增量 = 本版本 hash - 上一版本 hash，提取 `_compute_delta_hashes` 复用。
- 侧边栏「导出配置」4 个勾选排 2×2。
- 角色立绘贴图排除 `_e`/`_n` 法线/发光贴图（cardspine 1286→561，battlespine 560→190）。

### 7. 界面美化：新旧双主题 + 切换 + qtawesome 图标（已完成，已合并 PR #15；交互细节由 PR #20 延续）

> 对应 **Issue #17**（涉及共享文件 theme.py/main_window.py，已通气）。详见 `xl_updata_tool/docs/版本历史.md` v1.33.0。

- 保留旧主题，新增 Dracula 深色主题，侧边栏「主题」下拉框切换（QSettings 保存，重启保持）。
- 主要按钮加 Font Awesome 图标（qtawesome，fa6s solid）。

**设计规范（供对方过目，确保风格统一）**：
- 配色：Dracula 深色，主紫 `#bd93f9`、副粉 `#ff79c6`（focus/selection）、背景 4 档 `#1e1e2e/#25253a/#2e2e4a/#38385a`、文字 `#e8e8f0/#a0a0c0/#6c6c90`。
- 图标映射（fa6s）：检查更新=`arrows-rotate`、导入AS=`file-import`、刷新=`rotate`、作者=`user`、版本列表=`list`、图片预览=`image`、音频=`music`、角色=`users`。
- 后续渐进：继续清理剩余对话框/调试菜单的 emoji 与局部 QSS，让主题切换和多人协作边界更彻底。

### 8. 大批改进·阶段 A：bgm debank 修复 + 音频弹窗取消 + 反编译进度（已完成，已合并 PR #15）

> 对应 **Issue #18**（大批改进四阶段：A 明确bug → C 交互产物 → B UI设计 → D 性能立绘）。详见 `xl_updata_tool/docs/版本历史.md` v1.34.0。

- **bgm 没 debank**：`epic7_debank.py` `_should_process_file` 从「纯数字」改为「数字 或 bgm 目录」，bgm 79 个 bank 不再被过滤。
- **音频解密弹窗取消**：取消文字空串改为「取消」+ 接 `cancel` 信号。
- **反编译进度实时**：`progress_cb` 解析 done/total 接到进度条。
- 后续阶段（C 交互产物 / B UI 设计 / D 性能立绘）按计划继续。

### 9. 阶段 C/B/D（交互产物 + UI 设计 + 立绘拼接）（已完成，相关改动已分批合并 PR #15/#20）

> 对应 **Issue #18**。详见 `xl_updata_tool/docs/版本历史.md` v1.35.0 / v1.36.0。

- 阶段 C：手动开始按钮、进度弹窗加大、全量导出告知、边导出边预览、音频树形、lua/角色数据留存 + CHANGELOG。
- 阶段 B：侧边栏重排、图标去重、按钮统一、工具栏只在首页、下载按钮克制配色、新主题换蓝灰。
- 阶段 D：立绘拼接（UnityPy 提取背景偏移 + 偏移合成）+ 角色选择导出自定义。

### 10. 两轮测试反馈修复（已完成，相关改动已合并 PR #20；架构稳定性继续由 PR #22 收口）

> 详见 `xl_updata_tool/docs/版本历史.md` v1.36.0。

- 浅色主题、版本列表 checkbox 选中、角色数据本地缓存、图片/音频加载优化（分批 + 树折叠）、图片预览角色过滤、日志按启动分文件 + 计时。
- 修复 `_audio_player` / `_side_btn` / `QToolBar.addStretch` 三个崩溃。

---

## 二、UX 问题完整清单（从聊天记录 + 代码深挖，共 30 条）

> **已完成（见 `版本历史.md` v1.28.0 / v1.29.0）**：
> - 第一批（v1.28.0）：B1/G2、G4、E1、C1、E4、A4、D3。
> - 第二批（v1.29.0）：B2、A1、A2、F1、F2。
> - 可配置导出 + 音频分类：见 Issue #13。
> **G5 待修**：合成图拉伸变形，正确做法是用 SpineViewerCLI `merge` 命令合并导出（Issue #11），暂不手动拼图。

### A. 进度/状态反馈（用户感知不到系统在干什么）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| A1 | 导入AS无进度弹窗，进度条会假死 | `_import_selected` 2237，阶段1几百文件逐个修 | 细进度条长时间不动，以为程序死了 |
| A2 | 音频解密完全无进度条 | `_start_audio_decrypt` 2914，只状态栏文字 | 盯着空表格以为没反应 |
| A3 | 图片导出进度统计口径错误 | `PreviewExportWorker._do_export` 981，按 skel 计总但每个 skel 内部要导出几十个文件 | 进度条停在 1/N 很久 |
| A4 | 状态栏 12px 小字、瞬时覆盖 | theme.py 272 | 切走界面就看不到进度 |

### B. 后台运行与取消（切界面就中断，无法取消）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| B1 | 单文件导出/合成视频同步跑，冻结窗口数分钟 | `_export_with_dialog` 6102 / `_export_composite_video` 5668 | 窗口"未响应"，最坏卡 6 分钟 |
| B2 | 切走界面即杀后台任务 | `_toggle_preview_mode`/`_toggle_audio_mode`/`_toggle_character_mode` 都 cancel() | 换个界面导出就被取消 |
| B3 | 下载无法取消 | `DownloadWorker.stop()` 定义了但 UI 从不调用 | 只能干等 |
| B4 | 无"任务运行中"统一提示 | 全文件无 QProgressDialog | 切走界面不知道有任务在跑 |

### C. 错误处理（失败时用户看到"没反应"）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| C1 | CLI stderr 被吞 | `_run_spine_export` 2715 只 logger.debug | 导出失败只看到"没反应" |
| C2 | 删除文件静默失败 | `_delete_version` 6315 `except: pass` | 按提示删了重下仍失败，死循环 |
| C3 | 重新导入先清空整个 material 目录 | `_stage_export` 1794 rmtree | 新导入失败，旧资源全没了 |
| C4 | 下载错误只在状态栏一闪 | `_download_version` 2203 | "下载完成"却少了文件 |

### D. 功能触发时机（用户不知道点按钮会触发什么）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| D1 | "角色"按钮=视图切换+Lua反编译耦合 | `btn_lua` → `_start_lua_decrypt` 5022 | 只想看角色却触发几分钟反编译 |
| D2 | 图片预览入口与导出脱节，`_preview_images` 死代码 | 2359 从未调用，按钮叫"重新加载"实际触发导出 | 不知道点它会导出 |
| D3 | 右键菜单命名不一致、有空分支 | `_show_context_menu` 5476，5532 有 `pass` | "合成GIF/导出GIF"叫法不一，点了没反应 |
| D4 | 导出对话框动画下拉只有 idle | `ExportSettingsDialog` 323 硬编码 | 想导其他动作做不到 |

### E. 界面布局与状态（按钮无状态、进度条互相污染）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| E1 | 工具栏按钮无选中态、无高亮 | `_tbtn` 2007 未 setCheckable | 不知道自己在哪个界面 |
| E2 | 进度条跨操作复用 | `dl_progress` 被下载/导入/Lua三处共用 | 分不清进度条是哪个任务 |
| E3 | 音频"时长"列恒为 "-" | `_get_audio_duration` 5311 恒返回 None | 误导以为没加载完 |
| E4 | 批量导出只禁用一个按钮 | `_batch_export_with_dialog` 6203 | 能叠出多个任务 |

### F. 数据与文件管理（输出目录散乱、找不到文件）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| F1 | output/character 混放主图/动画/皮肤/图集/合成图 | 全堆一个目录 | 找图要翻几百张 |
| F2 | FGUI 图集散图倾倒进预览根目录 | `_export_fgui_atlas` 1147 | 几百张小图标淹没立绘 |
| F3 | 每角色几十张中间图永久保留 | `_export_animation_frames` + `_export_skel_skins` | 磁盘堆满、预览上千张卡顿 |
| F4 | 启动同步联网拉种子清单 | `_seed_bundled_version` 6325 同步 urlopen | 网络差白屏卡 45 秒 |

### G. 性能（用户可感知的卡顿）

| # | 问题 | 根因（方法） | 用户感知 |
|---|---|---|---|
| G1 | 角色数据解析在 UI 线程同步跑 | `_load_character_data` 3467 | 解密完成后卡死解析 |
| G2 | 单文件导出同步阻塞 | 同 B1 | 最直观的卡死 |
| G3 | 图片大图逐张全尺寸加载 | `ImageViewerDialog._load_current_image` 202 | 翻页卡顿、吃内存 |
| G4 | 缩略图中心裁剪 | `_create_thumbnail` 92 `KeepAspectRatioByExpanding` | 竖版立绘头脚被裁 |
| G5 | 合成图强制拉伸变形 | `_composite_images` 2499 `resize(bg.size)` | 角色被压扁拉长 |
| G6 | 图片预览同步 os.walk 建映射 | `_load_preview_images` 5354 | 上万文件时卡一下 |

---

## 三、优先级（按用户感知最痛排序）

1. **B1/G2**：单文件导出/合成视频同步冻结 → 全部后台化 + 进度窗
2. **B2**：视图切换即杀任务 → 任务与视图解耦 + 明确取消按钮
3. **A1/A2**：导入AS/音频解密无进度弹窗 → 统一 QProgressDialog
4. **D1**：角色视图切换 vs Lua反编译拆开，完成后弹通知
5. **E1**：工具栏按钮 checkable + 高亮
6. **G5/G4**：合成不拉伸、缩略图不裁剪
7. **F1-F3**：输出按角色/类型分目录，预览加过滤

---

## 四、开发计划（按阶段）

### P1：重构 main_window.py（已完成 ✅）

> 由 @zzzz259 负责拆分重构（Issue #2 已关闭），已合并进 main，核心流程验证通过。实际拆分结果见 `docs/版本历史.md` v1.25 / v1.26。其余模块见 Issue #3/#4/#5，待分工。

`main_window.py` 6356 行巨型单体，拆成：

```
app/ui/
  main_window.py          # 瘦身为窗口骨架 + 版本列表 + 下载 + 模块入口
  workers/                # 7 个 QThread Worker 独立
  dialogs/                # ImageViewerDialog / ExportSettingsDialog / 统一进度弹窗
  features/               # character_db / spine_export / fairygui_atlas
  adapters/               # AssetStudio / SpineViewer / ffmpeg / unluac 统一封装
```

重构的同时顺手解决 B 类（任务与视图解耦）、E2（进度条独立）。

### P2：UX 改进（按第三节优先级）

- 统一进度弹窗（QProgressDialog），覆盖下载/导入/导出/解密/反编译
- 任务后台化 + 明确取消按钮
- 工具栏高亮、错误信息可见、合成不拉伸、缩略图不裁剪
- 输出按角色/类型分目录

### P3：工具接入（替换 tools/ 旧工具）

| 工具 | 改动 | 替换路径 |
|---|---|---|
| AssetStudio.GUI.exe | 命令行目录自动加载 | `tools/AssetStudio/` |
| SpineViewerCLI.exe | merge 多模型导出 | `tools/SpineViewer/` |
| unluac_batch.jar | 目录批量 + 多线程 + 进度 | `tools/lua/` |

### P4：性能优化

- Lua 反编译：已从 15 分钟 → 30 秒，剩余瓶颈是文件 I/O
- 图片预览：异步缩略图 + 缓存上限
- 导入 AS：`_stage_export` 每种类型跑一次完整 CLI，合并为一次全类型导出

---

## Issue #32 回归修复记录（2026-08-19）

- 角色页切换改为只读本地角色仓库/缓存；现场 Lua 解析仅保留在“开始解析”和最新已发布 Lua 导出收尾流程。
- 自动角色解析的最新版本以 `output/lua/<版本>/` 实际存在的版本目录为准，不再使用尚未导出的版本数据库记录。
- 旧 `characters_full.json` 在首次建立版本仓库时作为基线，同版本重导使用规范化快照比较，避免整批角色重复显示“新”。
- 未读状态同步到顶部四个工作区，并在角色页提供“全部标为已读”。
- 仅勾选 Lua 时通过 `assets_map.json` 精确筛选含 Lua 的 AB，AssetStudio 使用只含命中包的临时输入目录；没有映射时保留兼容回退并写明警告。
- 导入进度弹窗延续到自动角色解析完成，用户可以看到导出后的收尾处理仍在进行。

## 五、分工建议（重构后按模块认领）

| 模块 | 建议归属 |
|---|---|
| app/core/（底层） | 共同维护，改动谨慎 |
| 版本管理 + 下载 | 一方 |
| 导入 AS | 一方 |
| 图片预览 + Spine/视频导出 | 一方 |
| 音频解密 | 一方 |
| Lua + 角色库 | 一方 |
| FairyGUI 拆图 | 一方 |

---

### Issue #35 实机修复：中日语音同名隔离（2026-08-21）

- 已确认中文语音不是 AS 阶段丢失，而是后处理的全局同名清理误删；修复后语音清理按角色 bank 与 `cn/jp` 作用域隔离。
- BGM 仍允许跨旧专辑目录清理，以保留活动 BGM 分类纠正能力。
- 已加入中日同名语音回归测试；本项不改变最终目录契约，不恢复 `data/material/assets/fmodassets/` 长期留存。

### Issue #35 定向修复：bank 缺口与并行 debank（2026-08-21）

- 每个 bank 改用唯一的临时提取/结果目录，避免中日同名 bank 和 064/095 角色编号互相污染。
- debank 默认 2 路受控并行，最终 output/audio 的复制、分类和旧文件清理保持串行。
- 新增 bank 级成功、空产出、失败和复制失败统计；E20 一类缺口将直接显示具体 bank 与来源路径。
- 重新导出时清理角色目录内的异号残留；080 的连续重复事件后缀按保守规则归一化为标准编号。
- 代码验证已通过，下一步等待用户执行真实全量导出，核对 E20、064/095、080 和实际耗时。

### Issue #35 第二轮修复：FSB fallback 与语音前缀归一化（2026-08-21）

- 已确认 E20 的旧 FSB 提取器在包含特殊字符的事件名上失败，QuickBMS 仍返回 0；新增状态文件传播，避免静默空产出。
- 已加入项目内 vgmstream fallback，并用真实 E20 FSB 验证成功输出 WAV；原提取器正常成功路径保持不变。
- 已确认 JP 064/095 的错位来自 FSB 内部统一前缀；仅在整个 bank 统一异号时按外层 bank ID 归一化，混合前缀不强行改名。
- 定向测试与代码门禁待本轮收尾后更新，用户仍需做一次真实全量导出。

### Issue #35 第三轮修复：BGM、取消控制与 bank 增量缓存（2026-08-23）

- 已定位并修复 BGM 复制前清理逻辑误删当前 `output/audio/.debank-temp/` 源文件的问题；BGM 仍按最新 Lua 专辑映射写入 `output/audio/album/`。
- 已修复音频“全部标为已读”只修改 Qt 数据副本的问题，叶节点及所有父级目录的“新”文字和颜色会同步清除。
- 音频取消现在贯穿 worker、bank 解包和 QuickBMS 子进程树；取消保留已发布文件，不自动续跑，替换旧任务前等待旧 worker 真正退出。
- 新增 `output/audio/.bank_state.json` bank 级缓存，校验来源指纹、分类目录和最终文件完整性；分类变化、文件缺失或强制导出会重新处理。
- 本轮全量 pytest 72 项通过、Ruff 通过、相关 Python 编译通过；下一步进行真实小范围音频链路验证，再根据日志评估并行度和 vgmstream 主路径是否值得单独立项。

### Issue #35 第四轮修复：bank 直解与 debank 提速（2026-08-23）

- 已从本地完整 AB 版本精准提取 1528 个音频 bundle，得到 1530 个 `.bytes`，确认当前软件的 AssetStudio 音频提取入口可直接提供 bank 素材。
- 已验证 vgmstream 直接读取 `.bank`：同批 12 个 bank 从 29.539 秒降至 4.031 秒；完整 735 bank 集成链路为 458.463 秒，712 个 bank 直解成功，23 个 SFX bank 进入旧链路后仍为空产出。
- debank 改为 bank 直解优先、QuickBMS/FSB 兼容回退；默认并行度调整为 4；同盘输出优先 `os.replace`，跨盘回退 `copy2`。
- 定向测试、完整音频实测和文档同步已完成；pytest 77 项、Ruff、compileall 和 diff 检查均通过，保留用户侧正式音频导出作为最终环境验收。

### Issue #35 第五轮修复：完整性审计与音频树交互（2026-08-23）

- 对照 `BaseSound.lua` 与 `BaseSoundChapter.lua` 建立 BGM bank 期望清单，并读取 `.bank_state.json` 验证缺失、产物失效和配置表之外的未分类项。
- 音频旧分类清理改为一次性文件索引，避免大批量导出时对每个 bank/文件反复递归扫描；默认 debank 并行度调整为 6，仍支持环境变量覆盖。
- 音频列表只在首次进入、显式刷新或后处理结束时重建；普通页面切换复用已有树。
- 音频树支持目录递归勾选、普通单选与 Ctrl 追加多选；播放进度条采用拖动预览、释放提交 seek。

### Issue #40：Audio Feature ownership 迁移 P1a（2026-08-24）

- 已新增 `app/features/audio/page.py`：音频页面自持控件，通过语义信号与迁移期 Shell 连接，不再由视图工厂绑定 `parent._xxx`。
- 已新增 Qt-free `AudioService`：统一 output/audio 目录扫描缓存、快照未读状态、全部已读和选中导出。
- 音频树逻辑已迁入 `app/features/audio/tree.py`，旧 `app/ui/features/audio_controller.py` 保留兼容转发；MainWindow 已接入新 Page/Service，但播放器、选择状态和 AudioDecryptWorker 留待 P1b。
- 本轮代码门禁：全量 pytest 98 项通过，Ruff `--no-cache` 通过，app/tests 共 109 个 Python 文件 AST 解析通过，`git diff --check` 通过。

### Issue #40：Audio Feature ownership 迁移 P1 完成（2026-08-24）

- 已将播放器、播放进度、音量、目录/文件勾选、右键菜单、选中导出和音频列表缓存迁入 `app/features/audio/controller.py`。
- 已将音频解密任务的创建、取消、进度弹窗和完成/取消/失败结果迁入 AudioController；`app/features/audio/worker.py` 作为功能域 Worker 入口包装现有解密实现。
- MainWindow 现在只保留音频页面显隐和导入总流程协调，不再直接导入或持有音频 Worker、播放器和音频控件状态。
- 音频输出、分类、增量缓存、未读标记和取消语义保持不变；旧模块暂保留兼容，避免一次性删除影响其他调用方。
- P1 代码门禁：全量 pytest 100 项通过，Ruff、AST 解析 111 个 Python 文件和 diff 检查通过；等待用户正常启动验证后进入 Characters Feature。

### Issue #41：Characters Feature ownership 迁移开始（2026-08-24）

- 已创建 GitHub Issue #41，范围限定为角色页面、角色控制器、数据恢复服务和 Wiki/未读/CSV 入口迁移。
- 当前确认角色解析与仓库逻辑已有稳定的 `app/core/character_*` 兼容模块，本轮先迁移调用和 UI 所有权，不改变 Lua 数据契约。

### Issue #41：Characters Feature ownership 迁移完成（2026-08-24）

- 已新增 `app/features/characters/page.py`、`controller.py`、`service.py`，角色页面、解析协调、未读和 CSV 入口已形成独立功能域。
- MainWindow 仅保留角色导航、页面显隐和导入总流程结果接入；旧角色工厂保留为兼容入口，Lua/仓库/Wiki/输出契约不变。
- 已补充角色功能域、架构边界和兼容入口回归测试；全量 pytest 100 passed、Ruff、AST 解析、真实 MainWindow smoke 和 diff 检查均通过，待用户正常启动确认后创建本地检查点。

### Issue #42：Versions/Download Feature ownership 迁移开始（2026-08-24）

- 已创建 GitHub Issue #42，范围限定为版本工作区、更新检查、下载计划、Bundle 状态同步、删除和导入选中版本适配。
- 已建立 `VersionPage`、`VersionController`、`VersionService` 和功能域 Worker 入口；MainWindow 正在切换为薄壳层。

### Issue #42：Versions/Download Feature ownership 迁移完成（2026-08-24）

- 版本页面、差异摘要、选择状态、更新检查、增量/全量下载、磁盘 Bundle 同步和删除已由 Versions Feature 管理。
- MainWindow 仅保留版本页面显隐、全局进度和导入流程的当前版本选择适配；旧下载 Worker/版本视图仍保留兼容入口。
- 全量 pytest、Ruff、AST 解析、真实 MainWindow smoke、文档同步和 diff 检查均通过，待用户启动验证后建立本地检查点。

## Issue #43：Importer Feature 与后处理注册表迁移开始（2026-08-24）

- 建立导出规格、ImporterService、ImportController 和兼容 Worker 边界，先收口职责，不改变资源输出目录、数据格式或用户可见流程。
- 保留现有精准 Bundle 筛选、AssetStudio 暂存发布、Lua 版本隔离、音频后处理和取消语义；完成后再进入 Preview Feature。

## Issue #43：Importer Feature 与后处理注册表迁移完成（2026-08-24）

- 已新增 `app/features/importer/spec.py`、`service.py`、`controller.py`、`postprocessing.py` 和 `worker.py`；导出分类、精准 Bundle 筛选、结果组装与后处理分类形成独立边界。
- MainWindow 已不再直接构造 `ImportASWorker`，只负责全局进度、导入弹窗和结果接入；旧 Worker 继续作为兼容执行器，未改变资源输出、Lua 发布、音频后处理和取消语义。
- 已完成全量 pytest、Ruff、AST、实际 Qt 启动冒烟、文档同步与 diff 检查；下一步建立本地检查点，用户验证通过后进入 Preview Feature。

## Issue #44：Preview Feature 与外部工具边界迁移开始（2026-08-24）

- 已创建 GitHub Issue #44，范围限定为图片预览、FGUI 图集、Spine 合成和视频导出边界。
- 先建立 Preview Page/Controller/Service/Worker 兼容入口，收口预览目录、筛选、导出任务和外部工具结果；不改变 output 目录、算法和旧 Worker。

## Issue #44：Preview P5a 页面与图片链路完成（2026-08-24）

- 已新增 `PreviewPage`、`PreviewService`、`PreviewController`、预览项构造和功能域 Worker 入口。
- MainWindow 已切换图片预览页面、目录扫描、角色筛选、缩略图加载和立绘导出任务生命周期；旧 `preview_view.py`、旧 Worker 和导出控制器保留兼容。
- 已完成定向测试、全量 pytest、Ruff、实际 Qt 启动冒烟与 diff 检查；用户验证通过后继续收口 FGUI、Spine 合成和视频导出。

## Issue #44：Preview P5b FGUI/Spine/视频导出边界开始（2026-08-24）

- 将把导出编排从 `app/ui/features/export_controller.py` 切换到 Preview Feature，旧路径保留兼容转发。
- FGUI 图集、Spine/FFmpeg 适配和批量 Worker 先完成 ownership 收口，不重写外部工具算法，不改变 `output/fgui`、`output/video` 和角色图片输出。

## Issue #44：Preview Feature 迁移完成（2026-08-24）

- 导出编排已切换到 `app/features/preview/export_controller.py`；旧 `app/ui/features/export_controller.py` 改为兼容转发。
- FGUI、Spine/FFmpeg 适配和批量/合成 Worker 已增加 Preview Feature 入口；外部工具参数、取消语义和 `output/character`、`output/fgui`、`output/video` 契约保持不变。
- 已完成全量 pytest、Ruff、AST、实际 Qt 启动冒烟、文档同步和 diff 检查；下一阶段进入 Platform/Shared 收缩。

## 六、发布前规划（新功能使用 + 环境打包）

> 详见 Issue #11，需双方讨论后再定实施顺序。

### 1. 新功能使用规划（merge，未来任务）

SpineViewerCLI 的 `merge` 命令（多 skel 合并导出）已就位，但 app 层还没调用。规划：

- 接入点：`export_composite_video_with_params()`（`export_controller.py`）+ `CompositeExportWorker._export_one()`（`composite_export.py`）
- 思路：把现有「角色导出 + 背景导出 + ffmpeg 叠加」三步，换成一次 `merge` 调用
- 复用：`find_composite_sources()` 配对 + `export_spine_media_file()` 命令模板
- 验证顺序：先验证旧功能不回归，merge 接入作为独立 feature 分支单独做

### 2. 环境打包方案（零配置部署）

现状需用户额外装 **Java 21+**（unluac）和 **.NET 8 runtime**（AssetStudio）。

方案：

| 依赖 | 方案 |
|---|---|
| Java（unluac） | `jlink` 精简 JRE（40-60MB）放 `tools/lua/jre/`，代码改用 bundled java；备选完整 JRE |
| .NET（AssetStudio） | `dotnet publish --sc true` 自包含发布（同 SpineViewerCLI） |
| Python + PySide6 | PyInstaller onedir（已配） |
| 部署 | 绿色便携文件夹 / Inno Setup 一键安装（需讨论） |

需讨论：JRE 精简 vs 完整、便携 vs 安装包、打包体积（ffmpeg 102MB + .NET 70MB + JRE）、tools/ 运行时是否进 git。
