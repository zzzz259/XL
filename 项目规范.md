# 项目规范

本规范适用于 XL monorepo。贡献、晋级与发布流程以 [CONTRIBUTING.md](CONTRIBUTING.md) 为准；正式文档入口见[文档地图](docs/README.md)。

## 仓库布局

```text
XL/
├── .github/workflows/  # CI 与发布自动化
├── docs/               # 仓库级文档索引
├── xl_updata_tool/     # Windows 桌面更新/资源工具
├── xl_updata_server/   # Linux 后台更新服务
└── xl_qqbot/           # QQ 群机器人
```

各子项目拥有自己的依赖清单、运行配置、测试和文档。不要假设仓库根目录有统一的 Python 项目或统一虚拟环境。

## Python 与运行环境

- 桌面工具开发默认使用仓库根目录 `.venv` 中的项目 Python；`run.bat`、`debug.bat` 优先使用 `xl_updata_tool/.venv`，不存在时回退至根 `.venv`。CI 可在工具子项目创建隔离 venv；Release 脚本默认在 `xl_updata_tool/build/.venv` 建构建环境。
- 服务端和 bot 的本地开发环境分别在自己的目录管理 `.venv`；不得把它们的依赖装进桌面工具环境。Bot 生产部署的 debug/test worktree 依照当前 systemd unit 共享主 Bot `.venv`，见 Bot 运维文档。
- 开发/测试依赖与运行依赖分开维护。运行环境版本、系统依赖和构建要求以当前 requirements、部署脚本及子项目文档为准。
- 本地凭据、`config.toml`、`.env`、下载数据、日志、缓存和导出产物不能提交。

## 文档规则

- 根 README 做仓库总览；子项目 README 做该项目的入口；详尽配置、架构、运维、开发、版本信息放在该项目的 `docs/`。
- 每个说明只保留一个事实来源，其他页面使用相对链接，不复制整段配置或实现细节。
- 文档必须区分“当前实现”“部署要求”“建议”“未验证”。需要准确记录默认值、路径、配置键时，以代码、配置示例和脚本为依据。
- 发布变更补入版本历史。计划草稿、聊天记录逐轮复述、临时审计报告不进入仓库正式文档。
- 第三方随附文档和工具历史日志默认原样保留，并明确其归属；升级供应工具时再按许可证要求同步。

## 代码与变更边界

- 新功能放入对应子项目和功能域；跨子项目改动需在 PR 中说明依赖关系和验证范围。
- Python 文件使用 UTF-8、4 空格缩进；新增公共接口尽量添加类型说明，错误信息应包含可行动的上下文。
- 临时文件放在专属工作目录或已忽略的临时目录。不可将计划文件、测试残留、下载的 AB、`output/` 成品、日志或个人机器绝对路径提交。
- 删除或重建数据前确认精确目标；测试必须使用隔离的临时数据，不能指向用户现有 `data/` 或 `output/`。

## 验证原则

- 使用所属子项目的 Python 环境，按其 README 执行测试、静态检查和构建检查。
- 自动化测试只证明其覆盖的行为。资源处理、GUI 交互、第三方 CLI、文件产物和线上服务还应提供针对原问题的人工或集成验证证据。
- CI 通过不等于已验证外部凭据、远端网络、真实 QQ/B 站接口或服务器部署。
