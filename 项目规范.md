# 项目规范（PROJECT STANDARDS）

> 本文档约定整个 XL 仓库的结构、文档、代码、命名规范。所有项目必须遵守。
> 协作流程见 [CONTRIBUTING.md](CONTRIBUTING.md)。

---

## 一、仓库结构（多项目 monorepo）

XL 仓库会放多个工具项目，每个项目一个独立子目录。

```
XL/                          ← 仓库根
├── README.md                ← 仓库总介绍（GitHub 首页自动显示）
├── CONTRIBUTING.md          ← 协作规范
├── LICENSE                  ← 许可证
├── .gitignore               ← 全局忽略规则
├── 项目规范.md              ← 本文件
├── docs/                    ← 仓库级共享文档（跨项目）
└── <项目名>/                ← 每个项目一个子目录
    └── ...
```

**规则**：
- 每个项目独立的子目录，互不干扰。
- 仓库根只放「总文档 + 各项目目录」，不放具体项目代码。

---

## 二、项目内部结构

每个项目遵循统一布局：

```
<项目名>/
├── README.md                ← 项目说明（安装/使用/功能）
├── main.py                  ← 程序入口
├── requirements.txt         ← Python 依赖
├── run.bat                  ← Windows 启动脚本
├── build.spec               ← PyInstaller 打包配置（统一命名，不叫 main.spec）
├── app/                     ← 源代码
│   ├── core/                ← 底层逻辑（数据库/下载/解析，不依赖 UI）
│   └── ui/                  ← 界面
│       ├── main_window.py   ← 主窗口
│       ├── workers/         ← QThread 后台任务
│       ├── dialogs/         ← 对话框
│       ├── features/        ← 功能模块
│       └── adapters/        ← 外部 CLI 封装
├── tools/                   ← 外部工具（AssetStudio/SpineViewer/ffmpeg 等）
├── docs/                    ← 项目文档（架构/开发文档）
├── data/                    ← 运行时数据（gitignore）
├── logs/                    ← 日志（gitignore）
└── output/                  ← 导出产物（gitignore）
```

**目录职责**：
- `app/core/`：纯逻辑，**不 import UI**，可独立测试。
- `app/ui/`：界面，依赖 core，不写业务逻辑（业务逻辑放 core 或 features）。
- `tools/`：编译好的第三方工具，不改源码（源码在别处维护）。
- `data/logs/output`：运行时生成，一律 gitignore。

---

## 三、文档规范

| 文档 | 位置 | 内容 | 读者 | 格式 |
|---|---|---|---|---|
| README.md | 仓库根 | 仓库总介绍 + 各项目导航 | 所有人 | md |
| README.md | 项目根 | 项目介绍 + 安装使用 + 常见问题 | 用户 | md |
| CONTRIBUTING.md | 仓库根 | 协作流程 | 开发者 | md |
| 项目规范.md | 仓库根 | 结构/代码/命名规范 | 开发者 | md |
| 开发文档 | 项目 docs/ | 架构/功能实现/调用链/验证方法 | 开发者 | md |
| 版本历史 | 项目 docs/ | 按版本记录功能、修复和结构变更 | 开发者/维护者 | md |
| 开发过程记录 | 项目 docs/worklog/ | 任务计划、调查发现、执行进度和验证过程 | 开发者/协作者 | md |

**规则**：
- **一律用 `.md`，不用 `.txt`**（GitHub 只渲染 md）。
- 每个项目的 README.md 必须含：项目简介、环境要求、安装步骤、使用说明、目录结构、常见问题。
- 用户文档和开发文档分开：README 给用户看，开发文档给开发者看。
- 开发文档必须包含**项目文件结构**，每次新增/修改功能后同步更新，保持与代码一致。

---

## 四、代码规范

1. **缩进**：4 空格（禁止 Tab）。
2. **编码**：UTF-8。
3. **命名**：
   - 模块/文件：`snake_case.py`
   - 类：`PascalCase`
   - 函数/方法：`snake_case`
   - 变量：`snake_case`
   - 常量：`UPPER_SNAKE_CASE`
4. **注释**：中文，解释「为什么」而非「是什么」。
5. **魔法数字**：抽成模块顶部常量，禁止裸写。
6. **类型注解**：公共函数/方法加类型注解。
7. **依赖方向**：core 不 import ui，ui 可 import core。
8. **单文件行数**：单个文件不超过 ~800 行，超过必须拆分。
9. **容错**：新增功能/流程加容错，单步失败重试 1 次，仍失败则跳过继续下一步，禁止因单步异常让整个流程停止。
10. **验证**：每次修改后运行 `python main.py`，确认无语法/逻辑错误再提交。

---

## 五、命名规范

| 对象 | 规则 | 示例 |
|---|---|---|
| 项目目录 | snake_case | `xl_updata_tool` |
| Python 文件 | snake_case | `bundle_parser.py` |
| 类 | PascalCase | `ImageLoadWorker` |
| 函数/方法 | snake_case | `download_bundle()` |
| 私有方法 | `_` 前缀 | `_load_preview_images()` |
| 常量 | UPPER_SNAKE_CASE | `FIXED_HEAD` |
| 配置/文档 | 中文或英文 | `开发文档指南.md` |

---

## 六、提交与 Git 规范

详见 [CONTRIBUTING.md](CONTRIBUTING.md)。核心：
- feature 分支 + PR，禁止直接 push main。
- commit 前缀：`feat/fix/refactor/docs/chore`。
- 测试文件放 `data/`、`output/`、`_tmp/`，自动忽略。
